# 코덱스 앱에 먹이는 법

OpenAI Codex 데스크톱 앱(macOS / Windows) 기준.
Linux 빌드는 없으므로 그 경우 IDE 확장이나 CLI를 쓴다.

---

## 파일 두 벌에 대하여

이 번들에는 지시서가 두 이름으로 들어 있다. **내용은 같다.**

| 파일 | 읽는 도구 |
|---|---|
| `AGENTS.md` | 코덱스 |
| `CLAUDE.md` | 클로드 코드 |

지금은 코덱스를 쓰므로 `AGENTS.md`가 읽힌다. `CLAUDE.md`와 `.claude/` 폴더는
그대로 둬도 무해하다. 나중에 도구를 바꿔도 다시 준비할 필요가 없다.

`src/impl/` 안에도 같은 쌍이 하나 더 있다. 그 디렉터리에서 작업할 때만
추가로 얹히는 좁은 규칙이다.

---

## 1. 폴더 준비

압축을 푼다. 선택할 폴더 바로 아래에 `AGENTS.md`가 보여야 한다.

```
tragedy-looper-mastermind/   ← 이 폴더를 선택
├─ AGENTS.md                 ← 바로 여기
├─ PROMPTS.md
├─ SETUP.md
├─ HANDOFF.md
├─ TODO.md
├─ gen.py
├─ data/  src/  test/
```

압축 프로그램에 따라 같은 이름 폴더가 한 겹 더 생긴다. 그러면 안쪽을 고른다.

git 초기화는 앱의 diff 검토와 워크트리를 쓰려면 필요하다. 첫 스레드에 시켜도 된다.

```
이 폴더를 git 저장소로 초기화하고 현재 상태를 커밋해라.
커밋 메시지는 "scaffolding: 기본편 상태 추적기 뼈대". 코드는 아직 건드리지 마라.
```

---

## 2. 앱에서 열기

1. 앱 실행 → ChatGPT 계정 로그인
2. 프로젝트 선택에서 위 폴더 지정
3. **Local 이 선택되어 있는지 확인** ← 놓치기 쉽다

Local이 아니면 클라우드에서 돈다. GitHub에 push 안 한 상태면 빈 손으로 시작한다.

---

## 3. 로딩 확인 — 반드시 먼저

```
AGENTS.md 의 "절대 규칙" 세 개를 그대로 인용해라. 아직 아무것도 고치지 마라.
```

인용을 못 하면 폴더를 한 단계 잘못 열었을 가능성이 높다.

여기서 안 잡으면 규칙을 지어낸 코드가 쌓인 뒤에야 발견하게 된다.
그때는 어디까지가 근거 있는 구현인지 구분이 안 된다.

---

## 4. 스레드 하나 = 작업 하나

`PROMPTS.md`의 블록을 0번부터 순서대로 하나씩 복사해서 넣는다.
하나가 끝나면 diff를 보고 커밋한 뒤 새 스레드를 연다.

### 워크트리는 당분간 쓰지 마라

0~6번 작업은 의존 관계가 직렬이다.

- 2번(행동 해결)은 1번(상태 초기화)이 있어야 한다
- 4번(역할 훅)은 2번이 있어야 테스트가 돌아간다
- 5·6번은 4번의 훅 구현에 의존한다

병렬로 돌리면 서로 다른 `GameState` 가정을 만들어 놓고 머지 때 충돌한다.
**7번(UI)만 따로 띄울 만하다.**

---

## 5. 네트워크 승인 — 0번에서 걸린다

코덱스는 기본적으로 네트워크를 막고 필요할 때 묻는다.
0번 작업의 `npm install`이 여기서 멈춘다. 승인하면 된다.

다만 **`gen.py` 실행은 절대 승인하지 마라.**
구현이 빈 스텁으로 되돌아간다. 승인 창에 `python gen.py`가 보이면 거부하고,
왜 실행하려 했는지 물어라.

> 클로드 코드에는 이걸 자동 차단하는 훅이 들어 있지만(`.claude/hooks/`),
> 코덱스는 그 파일을 읽지 않는다. **여기서는 사람이 봐야 한다.**

---

## 6. 훅 검증을 습관으로

`.claude/skills/verify-hook/SKILL.md`에 검증 체크리스트가 있다.
코덱스에서는 자동으로 안 불리므로 직접 시킨다.

```
.claude/skills/verify-hook/SKILL.md 의 절차대로 방금 구현한 killer 훅을 검증해라.
특히 3번 역방향 검사를 빠뜨리지 마라.
```

핵심은 역방향 검사다. 보통은 "원문 조건이 구현에 다 들어갔나"를 보는데,
실제 사고는 반대에서 난다 — 구현에는 있는데 원문에 근거가 없는 조건.
`alive` 체크나 장소 일치 확인을 습관적으로 끼워 넣는 게 대표적이다.
컴파일도 되고 테스트도 통과하는데 규칙이 틀린다.

반복 작업이니 앱의 스킬 기능으로 등록해두면 편하다.

---

## 7. 감시 포인트

앱은 diff를 보여주므로 커밋 전에 확인한다.

| 증상 | 대응 |
|---|---|
| `source`에 없는 조건이 `when`에 들어감 | "근거가 되는 source를 그대로 인용해라" |
| `gen.py` 실행 승인 요청 | **거부.** 구현이 스텁으로 되돌아간다 |
| `scenario.cast[...]` 직접 참조 | `effectiveRole()`로 교체 요구 |
| 훅 10개를 한꺼번에 채움 | 되돌리고 3~4개씩 다시 |
| "이 수가 좋습니다" 기능 추가 | 범위 밖. AGENTS.md "범위" 절 |
| `test/fixtures/*.json` 수정 | **가장 위험.** 픽스처는 공식 설명서다 |

마지막 항목을 특히 봐라. 에이전트는 테스트를 통과시키라는 압력을 받으면
테스트 쪽을 고치는 게 쉽다는 걸 안다. diff에 `manual-examples.json`이 보이면
무조건 되돌려라.

---

## 8. `QUESTIONS.md`

에이전트가 여기에 질문을 쌓기 시작하면 그게 **실물 설명서를 다시 봐야 할 목록**이다.

비어 있는데 훅이 다 채워졌다면 의심해라.
34개를 전부 무해석으로 구현할 수 있을 만큼 원문이 명확하지는 않다.
