# 트래지디 루퍼 각본가 보조 도구 — 인수인계

이 번들은 **다른 환경에서 개발을 이어가기 위한 출발점**이다. 여기서 완성된 것이 아니라,
결정된 사항과 자동 생성 가능한 부분을 정리해 둔 것이다.

---

## 1. 무엇을 만드는가

각본가 **혼자** 쓰는 도구. 주인공 화면 분리, 카드 뒷면 배치 연출, 정보 은닉 없음.
최종 목적은 계산된 근거를 바탕으로 **각본가의 전략 판단을 돕는 것**이다. 이를 위해
먼저 각본가가 규칙을 틀리지 않도록 상태 추적과 규칙 엔진을 완성하고, 그 위에 가설
공간과 정보 누출 예고를 쌓는다.

각본가는 이미 모든 진실을 알고 있으므로 추리할 게 없다. 실제 부하는 다른 데 있다.

- 9단계를 순서대로 굴리기
- 카운터 3종 × 캐릭터 9~11명 관리
- 「1루프당 1회」 카드 11장의 소진 여부 기억
- 금지 장소 · 이동 카드 중첩 정확히 처리
- 동시에 정보를 흘리지 않는 화법 유지 (각본가 설명서 11p)

이 중 앞의 넷은 상태 추적의 기반이다. **0~2단계는 상태 추적, 3단계는 가설 공간,
4단계는 정보 누출 예고이며, 5단계부터 전략 보조를 시작한다.**

---

## 2. 지원 범위

**기본편(basicTragedy)이 최소 요구선.** 입문편은 프롤로그에서 한 번 보여주는 용도라
입문편만 지원하는 도구는 실사용 가치가 없다.

캐릭터는 한국어판이 존재하는 26명 전체(본판 24 + 프로모 2).
확장 시나리오가 본판 외 캐릭터를 쓰는 경우가 있으므로 캐릭터 지원 범위와
참극 세트 지원 범위는 별개 축으로 둔다.

---

## 3. 단계별 로드맵

스코프를 자르는 축은 **콘텐츠 범위가 아니라 계층 깊이**다.

| 단계 | 계층 | 산출물 |
|---|---|---|
| 0 | 상태 추적 | 9단계 상태 추적, 카운터·소진 카드 자동 관리 |
| 1 | 상태 추적 | 합법수 검증, 이동 합성, 사건 발생 판정 |
| 2 | 상태 추적 | 패배 조건별 잔여 거리 |
| 3 | 가설 공간 | 룰 조합, 역할표, 사건 범인표의 후보 제약과 확정 전파 |
| 4 | 정보 누출 예고 | P5·P6·P9 선택 전후의 가설 공간 변화와 공개 정보 비교 |
| 5 | 전략 보조 | 합법적인 선택지별 승리 진척과 정보 누출을 계산해 비교 |

0~2단계는 가설 공간 없이 독립적으로 완결된다. 3단계가 무너져도 앞은 그대로 쓰인다.
4단계는 3단계의 변화를 선택 전에 계산하며, 5단계는 0~4단계의 결과를 근거로
각본가에게 선택지와 예상 결과를 보여준다.

### 5단계 전략 보조

아직 구현하지 않는다. 구현할 때는 모든 선택지를 `src/engine/legal.ts`로 검증하고,
패배 조건까지의 거리·정보 누출량·가설 공간 변화를 계산 근거로 함께 보여준다.
각본가의 판단을 대신하거나 불완전 정보에서 하나의 수를 "최적"이라고 단언하지 않는다.

- **루프 시작 선택 조언**: 하수인 배치와 학자 카운터 선택의 승리 진척·정보 누출 비교
- **각본가 능력 발동 판단**: 발동과 미발동의 승리 진척·정보 누출 비교
- **카드 배치 조언**: 합법적인 배치 선택지와 각 선택의 예상 결과 비교
- **미끼 전략 지원**: 여러 패배 조건을 동시에 성립시켜 주인공의 가설을 분산시키는
  선택지와 노출 위험 비교

#### 상태공간 feasibility 게이트

전략 UI보다 canonical 상태공간 feasibility 계측을 먼저 한다. 검색 키는
`engineStateKey`와 `protagonistPolicyStateKey`의 두 계층이다. 전자는 현재 전이
상태·과거 전이 요약만 가지며 물리 전이를 공유한다. 후자는 공개 이력을 읽는 정책에만
붙는다. 기본 정책은 현재 모든 합법 대응 중 각본가에게 가장 불리한 결과를 비교하는
`worst-legal-response`라서 정책 키가 없다. 완전 공개 trace는 `perfect-recall` 진단과
정보 누출 평가에만 남긴다. `tools/phase5-feasibility/`에 프로덕션 번들과 분리된
수집기와 headless 전이 열거기가 있다.

이 선택은 명세의 최악 기준 비교와 맞고 확률·승률·몬테카를로·대표 대응·숨은 가중치를
쓰지 않는다. 얻는 것은 물리 상태와 전이 캐시의 병합 및 모든 합법 대응에 대한 보수적
관계다. 잃는 것은 실제 주인공 대응 예측과 시그널링 효과 평가다. 후자는
`disclosure-preview` 기반 별도 축으로 표시하고 물리 탐색 점수에 섞지 않는다.

새 공개 기록은 `PublicInformation`과 공개 `PhaseLogEntry`가 공유하는
`(loop, day, phase, sequence)`를 가진다. 역할 공개 순간 공개 보드도 저장해 불안 3
이상에서 `person`이 공개되면 `paranoiaVirus` 조합을 배제한다. 같은 시점의
`serialKiller` 공개는 기본 `person` 변이일 수 있으므로 배정 역할로 과잉 확정하지
않는다. 시점 상태가 없는 구 저장은 새 배제에 쓰지 않는다. 전수 감사 결과는
`tools/phase5-feasibility/observation-order-audit.md`에 있다.

Section 1의 full-trace 판정은 정책이 정해지지 않은 `perfect-recall` 범위에서는 여전히
유효하다. 제한된 2일 36경로 감사에서 full trace 병합률은 0%, trace 없는 물리 상태
병합률은 97.22%였다. 이것이 2계층 전환의 근거다.

Section 2 측정 프로필은 2026-08-20에 승인되었다. `firstSteps:2` 최초 계측은 초기
리더 3배와 P2 제출 순서 6배를 잘못 canonical 분기로 보아 폐기했다. v2 재검증은
root 1개, P2 63,360개, P3/부모 368,640개를 확인했고 다음 계층은 정확히
23,357,030,400개·병합률 0%라 1억 하한 중단 조건에 걸렸다. P3 수에는 제출 순서
6배가 없었다. 실제 집합 감사에서 `(owner,card,target)`은 368,640개 모두 고유했고,
owner를 지울 때만 61,440개로 6배 축약됐다. 그 6배는 시간 순서가 아니라 세 소유자와
세 대상의 대응이며, P3가 새로 만드는 플레이어별 1회 카드 소진 때문에 일괄 제거할
수 없다. 완전 무효 프로필을 양측에서 모두 제외해도 3.64%만 감소하므로 규모의 주된
원인은 무의미한 수가 아니라 독립된 세 주인공 손패와 owner별 자원이다. 별도 프로세스·
별도 임시 디렉터리의 독립 두 실행 SHA-256이 일치했다. 결과는
`tools/phase5-feasibility/results/firstSteps-2.md`에 있다. 다른 시나리오 계측은
중단된 상태다.

2계층 재계측에서는 한 P2 부모의 합법 P3 368,640개를 P4까지 전부 실제 해결했다.
`engineStateKey` 후속 상태는 12,062개로 병합률 96.728%였고, 물리 상태당
perfect-recall 정보 문맥은 평균 30.56개·중앙값 6개·최대 13,584개였다. 독립 두 실행
SHA-256 `9abab4a7...993db`가 일치했다. 다만 전체 P2×P3의 해결 전 합법 간선
23,357,030,400개는 그대로여서 원시 경로의 명시적 열거는 중단 조건을 넘는다.

후속 목적지 CSP/DP 계측은 원시 간선을 만들지 않고 전체 P2의 P4 목적지를 완전
열거했다. 위치·카운터만 6,632,690개, 주인공 소진 자원을 잔량으로 합친 근사 키는
12,594,452개, 현재 소유자별 정확 엔진 투영은 34,516,856개다. 잔량 키가 물리 키보다
큰 것은 자원 필드를 추가했기 때문이며, 소유자별 키보다는 작다. P2 100개 물리 합집합은
158,478개로 전체의 2.389%였다. `6,182`·93% 포화 수치는 생성 코드와 상태 키가 Git 및
잔존 임시 산출물에 없어 재현 불가로 폐기했다.

따라서 상태 집합의 목적지 열거는 가능하지만 전략 탐색에는 상태만으로 부족하다. 다음
게이트는 `결정 상태 × 합법 행동 동치류 → 후속 canonical 상태`의 상징적 전이 관계다.
모든 계측은 앞으로 실행 코드·키 정의·독립 재실행 hash·결과 문서를 함께 커밋해야 한다.
상세 결과는 `tools/phase5-feasibility/results/firstSteps-2-engine-state.md`와
`tools/phase5-feasibility/results/firstSteps-2-p4-destinations.md`에 있다.

행동 동치류 게이트를 재현 가능한 코드로 계측했다. 해결 전 `placed`를 포함하는 문자적
후속 상태에서는 P2 63,360개와 고정 P2의 P3 368,640개가 전부 구분된다. P4까지 닫고
소유자별 소진 자원을 보존하면 초기 P2는 15,558 함수류이고, 고정한 P2 10개의 P3는
9,308~12,504류다. 실제 엔진으로 만든 2일차 대표 9상태에서도 P2 4,795~16,513류,
상태별 P2 10개에서 P3 3,559~12,504류였다. 동치류는 수백 개가 아니라 수천~만
단위이므로 둘의 곱을 명시적 간선으로 만들지 않는다. 완전 opening book과 전체 상태
공간 사전 열거는 불가로 확정했고, `feasibility_gated`는 벤치마크 실패 상태로 충족했다.
연구 가설은 남기되 통과하는 새 벤치마크 전에는 휴리스틱을 제거하지 않는다. 독립 두
실행 hash는 `ecb71ce6...0cb9e`로 일치했다. 상세 결과는
`tools/phase5-feasibility/results/firstSteps-2-action-equivalence.md`에 있다.

고정 P2 하나의 1수 앞 전경 탐색은 원시 P3 368,640개를 10,567 P4류로 만든 뒤 실제
엔진 후속 상태와 `distanceToLoss` 축별 최악값을 계산하는 데 2.53초였다. 후보 10개
선형 환산은 25.3초다. 현재 구현은 폰 즉시 비교 범위로 판정할 근거가 없으므로 날짜
종료·다일 계측은 보류한다. 물리 P4 동치류는 공개 trace를 보존하지 않으므로 정보 누출
평가에 그대로 쓰지 않는다. 상세 결과는
`tools/phase5-feasibility/results/firstSteps-2-one-ply-search.md`에 있다.

기존 `distanceToLoss` 분산 89%/94%/94%는 2026-08-21 감사에서 수치만 재현되고
전략적 판정은 폐기됐다. `role:keyPerson:*`는 핵심 인물 음모 거리가 아니라 현재 사망
여부 0/1이었다. First Steps 2 원문의 핵심 인물 사망 경로(청부업자 동소+음모,
연쇄 살인마 단둘, 3일차 자살)와 청부업자 주인공 살해를 별도 축으로 재계측하니
1·2·3일차 모두 불변 후보가 0/100이었다. 따라서 “대부분 대응이 무의미”와 이를 전제한
날짜별 탐색 설계는 무효다. 근사 표본 조기 종료를 쓰지 않는 안전 판정만 유지한다.
2·3일차 상태 선택기도 불완전한 벡터의 사전식 순서를 사용했으므로 기존 상태는 수치
격리 재현용일 뿐 더 이상 임계 스트레스 대표로 간주하지 않는다.
번들 47개 난이도의 반환 계약 12종 전수 감사에서는 계약 불일치와 예외가 모두 0건이었다.
다만 `keyPerson`·`friend`는 죽음의 종착 상태만 재고, 복수 요구조건의 최상위
`remaining`은 축 결손을 합산하므로 탐색 소비자는 `requirements`와 별도 사망 경로를
읽어야 한다. 상세 A/B 결과는
`tools/phase5-feasibility/results/firstSteps-2-loss-axis-audit.md`에 있다.

`distanceToLoss`는 2026-08-21부터 실제 패배 판정 종착 조건과 화면용 `routes`를 함께
반환한다. 판정의 timing·activation·`met`은 유지하고, 위험 경로는 자동/각본가 선택/
주인공 선택, 발동 시점, 개별 요구조건을 보존한다. 핵심 인물·친구에는 현재 각본의
청부업자, 연쇄 살인마, 자살·살인 사건·원격 살인·병원 사건, 이세계인 우호 4 경로를
붙인다. 보호 카운터, 시간 여행자 사망 면역, 사건 억제와 검은 고양이 효과 없음도
경로 요구조건이다. 살인 청부업자 카드에는 본인 음모 4 주인공 사망과 핵심 인물
살해를 함께 표시한다. 연인A·병원 사건은 요구조건별 행을 사용하며 UI는 최상위
`remaining` 합계를 읽지 않는다. 번들 47개 난이도에서 142조건·224경로를 감사해
계약/경로 불일치와 예외가 0건이었고, `firstSteps:2` 390×844 실제 화면에서 5경로·
25요구조건, 가로 넘침과 브라우저 오류 0건을 확인했다. 상세 결과는
`tools/phase5-feasibility/results/loss-risk-routes-ui.md`에 있다.

시나리오 선택 화면의 게임 시작 버튼은 지침보다 위에 둔다. 지침은 기본 닫힘이며
`게임 전 준비 · 게임 중 다시 확인`으로 표시하고, 게임 중 각본가 정보 패널에서도
같은 A~E 내용을 접어서 다시 볼 수 있다. 선택한 시나리오의 `룰과 역할`과 `사건 일정`은
게임을 시작하기 전에도 비공개 정보표로 바로 표시하며, 시나리오·난이도 선택을 바꾸면
함께 다시 계산한다. 선택 화면에서는 룰 후보 이름 목록, 캐릭터 특성·시작 선택값,
사건의 D-day·조건 상세처럼 각 행 아래 붙는 작은 보조 문구를 생략하고 핵심 값만
표시한다. 게임 중 패널에는 이 상세를 그대로 유지한다. 게임 시작 버튼은 이 정보표보다
위에 유지한다.

시간의 틈에서 `최후의 싸움으로 이동`은 즉시 상태를 바꾸지 않고 확인 모달을 연다.
남은 루프를 생략해 주인공 측이 불리해진다는 경고는 시간의 틈 본문에 상시 반복하지
않고 모달 안에 표시하며, `그래도 이동`을 한 번 더 눌러야 실제 전환한다.

게임 전 각본가 지침 A는 이 224개 기본 위험 경로를 정적 계산 입력으로 사용한다.
필요 카운터를 실제 각본가 손패의 1루프당 1회 카드와 보조 능력 횟수로 나누고,
같은 대상에 하루 한 장만 놓을 수 있는 제약·사건 예정일·마지막 날·루프 종료 판정을
최단 일수에 반영한다. 주인공 선택 경로는 순위에서 제외한다. 각본가가 노릴 수 있는
경로는 `1순위 / 2순위 / 3순위`로 통일하고 나머지만 별도 접기에 넣는다. 2순위부터는
1순위와 카드·능력 자원 교집합이 작은 경로를 앞에 둔다. `자동 발동` 통제 표기가 이미
있으므로 같은 강제 발동 경고를 각 행과 하단에서 반복하지 않는다. 시간 여행자는 행동
0회이며 주인공이 우호를 3개 놓지 않고 방치하면 마지막 날 성립한다고 명시한다.

조건부 능력을 놓치지 않기 위해 변수의 도심 음모 2 핵심 인물 능력과 망상 확대
바이러스의 엑스트라 불안 3 연쇄 살인마 변이를 별도 정적 투영한다. 하수인의 시작
장소는 임의로 하나를 고르지 않고 네 합법 장소를 모두 계산한다. 이 투영과 동일 인과
경로의 서로 다른 패배 종착점을 보존하면 47개 난이도의 화면용 지침은 316행이며,
224개 기본 경로 계약은 바꾸지 않는다. 공식 기본편 8편의 승리 메커니즘 36/36을
회수했지만, 정보 은닉·블러프·루프별 계획을 쓰지 않는 A의 1순위는 공식 지침과
4/8만 같거나 모순이 없다. 이 차이를 숨은 가중치로 보정하지 않는다. 상세 결과는
`tools/phase5-feasibility/results/mastermind-guidance-a.md`에 있다.

게임 전 각본가 지침 B는 A 아래에서 시나리오의 실제 캐스트 역할·캐릭터 특성,
선택된 룰, 예정 사건만 정적으로 골라 `정체가 드러나는 행동`, `자동 발동 주의`,
`사건·운영 체크`, `주인공이 쓸 수 있는 수단` 네 접기로 표시한다. 역할 조건은
`캐릭터 · 역할`로 직접 적는다. 절대 우호 무시와 시간 여행자는 실제 역할 보유자의
이름을 붙인다. 거부 불가 우호 능력은 역할과 무관하게 언제나 해결되므로 정체 노출로
세지 않는다. 특히 간호사는 절대 우호 무시 여부가 우호 능력으로 드러나지 않는다.
주인공 수단은
위험하다고 선별한 일부가 아니라 캐스트의 랭크가 있는 우호 능력 전부를 표시하며,
실제 역할에 따른 `반드시 거부 / 각본가가 거부 가능 / 거부 불가`도 함께 계산한다.

강제 사망·역할 공개·연인 불안 6·인과율과 함께 검은 고양이, 환상, 교주, 신수,
지연 등장 특성을 자동 발동 주의로 경고한다. 예정 사건, AI 사건 판정, 변수의 조건부
능력, 학자·하수인·거물처럼 각본가 선택이 개입하는 항목은 사건·운영 체크로 분리한다. 특히
사건이 핵심 인물을 먼저 죽여 청부업자 경로를 없애거나, 연쇄 살인마가 핵심 인물을
죽여 같은 날의 다른 계획을 중단하는 경우를 별도 위험으로 강조한다. 현재 번들에
실제 등장하는 특성 원문은 요청서의 11종이 아니라 모방자까지 12종이고, 선택 룰도
16종이므로 더 좁게 누락하지 않고 모두 감사했다. 47개 난이도 모두 생성됐고 캐스트
우호 능력 누락 0건, 공식 8편의 B 주의 메커니즘 30/30 일치, A 우선순위 차이 4편의
B 요소 4/4 회수다. 390×844 게임 전·게임 중 실제 경로에서 세 접기, 가로 넘침 0,
브라우저 경고·오류 0을 확인했다. 상세 결과는
`tools/phase5-feasibility/results/mastermind-guidance-b.md`에 있다.

게임 전 각본가 지침 C는 A의 우선순위를 바꾸지 않고 `시선 분산과 미끼`를 별도
정적 정보로 붙인다. 선택 룰이 속성을 요구하면 캐스트의 같은 속성 전원과 실제 역할
보유자·미끼 후보를 분리한다. 현재 지원 세트에서는 `signWithMe`의 소녀 조건이
활성화된다. 공개 확장 데이터의 룰·역할 원문도 전수 검색해 정적 배정 조건 7건과
동적 성별·태그 판정 7건을 감사 카탈로그로 보존했으며, 아직 지원하지 않는 참극 세트의
항목은 화면에 활성화하지 않는다.

16개 지원 룰은 공개 관측 유형으로 분류한다. 관측을 먼저 보여주고 그 아래 목록에
이번 게임의 룰과 같은 시트에서 그 관측을 설명할 수 있는 다른 룰만 나열한다. 실제
룰은 목록의 `이번 게임` 표기로 충분하므로 목록 위에서 이름을 반복하지 않는다. 다른 참극
세트의 유사 관측은 같은 시트 추리에 섞지 않는다. 가짜 패배 조건은 선택되지 않은
룰·역할의 요구 상태를 투영한 뒤
기존 `explainableLossConditions()`가 실제 설명 후보로 돌려주는 경우만 표시한다.
핵심 인물 계열은 사망 직후 종료 여부가 즉시 관측되므로 가짜 패배 조건에서 제외한다.
살인 청부업자·연인A는 현재 각본에 실제 주인공 사망 전달 수단이 있을 때만 미끼로
표시한다. 장소 음모와 캐릭터 음모는 계산에서 서로 다른 대상 종류로 보존하지만 UI의
가능 대상만으로 구분되므로 대상 종류 칩은 표시하지 않는다. 장소 음모 수단은 각본가
`음모+1/+2`, 실제 흑막, 캐스트의 기자 [우호2], 선택된 불온한 소문, 등장한 검은
고양이만 현재 시나리오 조건에 맞춰 표시하고 각본가·주인공·자동 통제를 함께 적는다.

번들 47개 난이도에서 예외 없이 생성된다. 과거 감사의 다른 참극 세트 관측 짝과
핵심 인물 계열 미끼 수치는 현재 표시 계약에 적용하지 않는다. 공식 기본편 8편에서 C 범위의
보조 정보가 8/8 생성됐으며, 특히 A의 공식 우선 지침 차이 4편에 있던 정보 은닉·블러프
근거를 별도 층으로 채웠다. 390×844 실제 UI 검증 결과는 해당 감사 문서와 함께 유지한다.
상세 결과는 `tools/phase5-feasibility/results/mastermind-guidance-c.md`에 있다.

게임 전 각본가 지침 D는 `끝까지 지킬 후보`를 정적 순서로 계산한다. 역할이 없는
엑스트라는 은폐 후보에서 제외한다. 각 캐릭터의
실제 역할은 `effectiveRole()`로 읽으며, 역할 고유 발동과 캐스트에 실제로 존재하는
역할 공개 우호 능력을 별도 노출 경로로 센다. 노출 경로마다 자동 강제/각본가 선택/
주인공 선택, 회피 가능성, 회피 방법, 그때 포기하는 능력·배치·승리 경로를 표시한다.
무녀 [우호5]처럼 여러 후보에 공통으로 적용되는 외부 역할 공개 능력은 후보마다
반복하지 않고 `공통 역할 공개 수단`에 한 번만 표시한다. 사용자와 대상이 같은 장소에
갈 수 있는지도 계산하므로, 도심에 갈 수 없는 무녀는 도심을 떠날 수 없는 AI의 역할을
공개할 수 없다는 예외가 드러난다.
마녀처럼 능력은 없지만 거부 가능한 우호 능력이 있는 캐릭터라면 강제 거부로 드러나는
경우는 기본 역할 난이도와 실제 시나리오 난이도를 분리한다. 환자처럼 우호 능력이 없는
캐릭터는 이 강제 거부 관측이 생기지 않는다.

흑막과 선동가는 능력을 쓰지 않으면 숨길 수 있지만, 현재 A가 계산한 음모·불안 필요
승리 경로 중 몇 개가 영향을 받는지 함께 센다. 청부업자, 시간 여행자의 우호 금지
무시·사망 무효, 친구, 연인,
연쇄 살인마, 변수와 망상 확대 바이러스의 엑스트라도 각 원문 발동 조건에 맞는 교환을
갖는다. 정렬은 이미 공개됐는지, 자동 강제 경로 수, 주인공 공개 경로 수, 각본가 선택
경로 수, 포기할 현재 승리 경로 수를 사전식으로 비교하며 숨은 가중치나 `최적` 표현을
쓰지 않는다. 초반에는 룰 가설 은폐, 종반에는 승리를 위한 노출 감수, 최후의 싸움에서는
한 명만 남기는 최종 방어선이라는 서로 다른 목적도 화면에 분리했다. 입문편은 최후의
싸움이 없으므로 룰 은폐 비용 비교로만 표시한다.

번들 47개 난이도에서 모든 비엑스트라 역할 보유자의 순위가 예외 없이 생성됐고, 공식 기본편 8편의
`mastermindHints`에 있는 은폐·의도적 노출 메커니즘을 8/8 회수했다. 공식이 최종
후보 역할을 직접 지정한 5편 중 D의 정적 1순위까지 같은 것은 1편이다. 나머지는 공식의
루프별 운영 의도가 정적 노출 경로 순서를 덮는 차이이며, 이를 숨은 가중치로 보정하지
않고 감사 결과에 그대로 남긴다. `못된 고양이`는
정적 번들 제목에 없으므로 저장된 사용자 각본의 직접 재현과 별도로, 같은 판단을
요구하는 연쇄 살인마 포함 각본에서 “단둘이 강제 사망을 피하면 사망 승리 경로를
포기하며 끝까지 지킬 후보로는 후순위”가 생성되는 일반 계약을 고정했다. 상세 결과는
`tools/phase5-feasibility/results/mastermind-guidance-d.md`에 있다.

게임 전 각본가 지침 E는 모든 루프가 공유하는 관측 전 1일차 시작 상태에서 한 번만
개시 배치를 계산한다. P2 전체 63,360개를 만들지 않고 A의 1순위·대안에 필요한 카드
대상과 C의 1일차 즉시 진척 미끼만 원자 후보로 만든다. 장소 음모와 캐릭터 음모는
`Target.kind`로 분리하고, 이동 후보는 시작 장소에서 목표 관계를 실제 한 칸 진척하는
방향만 남긴다. D는 후보 생성에 관여하지 않으며 보장·무대응 진척이 같은 조합에서
능력 대신 카드로 역할 노출을 아끼는 정도만 동률 평가한다.

224개 기본 위험 경로를 장소 음모 63, 캐릭터 음모 78, 불안 92, 위치 관계 118,
기타 16으로 다시 감사했다. 한 경로가 복수 축을 가질 수 있어 합계는 224보다 크다.
번들 난이도별 기여 원자 배치는 12~37개, 합법 3장 조합은 40~2,652개였고 47개 전체
계산은 약 0.9초였다. C의 가짜 조건 270건 중 198건만 1일차 카드로 진척할 수 있어
후보로 남긴다. 사망·낮은 우호처럼 1일차 카드로 직접 진척되지 않는 미끼의 제외 이유와
전체 검토 수는 감사·회귀용 계산값으로만 보존하고 시작 지침에는 표시하지 않는다.
추천된 모든 카드가
실제 해결기에서 표시 축을 진척하는지, 이동 금지·불안 -1·정확히 한 장의 음모 금지를
쓴 최악 대응 뒤 보장값이 맞는지 47개 난이도에서 전수 대조해 불일치 0건이었다.

1일차 전수 비용은 충분히 작지만 2일차부터는 주인공 카드와 공개 결과에 따라 실제
보드 상태가 갈라져 게임 전 고정 추천이 진행과 어긋날 수 있으므로 E 범위에서 제외한다.
이 계산 범위 설명은 화면에서 제거한다. 공식 기본편 8편의 첫날 내용과
대조하면 생산적 카드 축은 일부 회수하지만, 공식의 블러프·우호 금지·후속 날짜 운영은
의도적으로 후보가 아니다. 대상 종류는 계산 계약에만 남기고 카드의 가능 대상만으로
구분되는 UI 칩은 표시하지 않는다. 차이를 숨은 가중치로 보정하지 않는다. 상세 결과는
`tools/phase5-feasibility/results/mastermind-guidance-e.md`에 있다.

후속 감사에서 `disclosure-preview`의 `distanceToLoss()` 의존은 선택형 조건의 key,
activation, category 조회 한 곳뿐이고 거리·요구조건·경로를 정보 누출량으로 쓰지 않음을
확인했다. 가설 필터는 `distanceToLoss()`를 참조하지 않고 공개 종착 관측만 사용한다.
따라서 142개 종착 조건과 224개 비공개 인과 경로는 다른 계층이며, 경로를 공개 가설
조건으로 늘리지 않는다. 기본편 실제 각본 2편을 역할 훅→사망→즉시 종료→공개 관측까지
진행한 회귀에서 실제 룰 조합 배제는 0/2였다. 요청한 15개 대표 경로의 성립 직전·성립·
미성립과 보호·불사·검은 고양이·하수인 사건 억제도 테스트로 고정했다. 상세 결과는
`tools/phase5-feasibility/results/disclosure-hypothesis-loss-route-audit.md`에 있다.

축별 정적 관련성 계측에서 기존 10,567 P4류 중 핵심 인물 음모와 청부업자 위치·양쪽
음모 합집합은 2,295류였다. 무관 결과 대표 1개를 포함한 2,296회 실행으로 전체 축
결과를 정확히 재현한다. 다만 연쇄 살인마의 단둘 조건을 위해 모든 생존자 위치를 같은
합집합에 넣으면 7,165회가 필요하다. 따라서 축별 독립 최악값 탐색과 전이 캐시 공유를
기본으로 하고, 모든 사망 경로를 한 합집합으로 만들지 않는다. 원시 P3 2,244,960개를
정적으로 분류하고 P4 동치류 65,892개를 실제 실행한 전수 대조에서 상징/실제 불일치,
무관 오분류, 실제 결과 누락은 모두 0건이었다. 기존 `role:keyPerson:*` 거리는 핵심 인물
음모가 아니라 현재 사망 여부이므로 새 계측은 원문 훅의 필드 계약을 별도로 쓴다.
상세 결과는 `tools/phase5-feasibility/results/firstSteps-2-axis-relevance.md`에 있다.

---

## 4. 엔진 구조를 결정한 세 가지

기본편에서 처음 등장하는 메커니즘인데, 나중에 얹기가 매우 까다롭다.
**처음부터 이 구조로 짜야 한다.**

### ① 역할은 상수가 아니라 상태의 함수다

망상 확대 바이러스(`paranoiaVirus`): 엑스트라에 불안 3개 이상 → 연쇄 살인마로 변이.

`scenario.cast[c]`를 직접 읽는 코드를 뿌려두면 전부 고쳐야 한다.
반드시 `effectiveRole(state, char)` 한 곳으로 통과시킬 것. → `src/types.ts`

### ② 루프는 독립적이지 않다

인과율(`threadsFate`): 직전 루프 종료 시 우호 카운터가 있던 캐릭터 전원에게 불안 2개.

`LoopState`를 매 루프 버리면 구현 불가. `GameState.history`에 스냅샷을 쌓는다.
지금 안 쓰더라도 지금 쌓아두지 않으면 3단계에서 관측 이력을 복원할 수 없다.

### ③ 장소 X는 루프마다 바뀔 수 있다

거대 시한폭탄 X → 마녀의 시작 장소. 그런데 하수인(`henchman`)이 마녀를 맡으면
각본가가 매 루프 시작 장소를 지정한다(각본가 설명서 FAQ Q11).

상수가 아니라 `resolvePlaceX(state)`. → `src/types.ts`

---

## 5. 3단계 가설 공간 — 계층화가 답이다

```
룰 조합       입문편 9   →  기본편 105
전체 가설     캐스트 9명 기준 1,971,072
  최대  살인계획 + 친목동아리/연애의풍경  = 181,440
  최소  거대시한폭탄 + 인과율/불확정인자  =      72
```

기본편 스크립트 22편 중 13편이 캐스트 9명이므로 이게 표준 케이스다.
200만 개를 매 라운드 필터링하는 것은 순진하게 짜면 안 돌아간다.

**두 층으로 나눈다.**

- **상층: 룰 조합 105개** — 완전 열거, 매 라운드 갱신, O(105)
- **하층: 캐릭터 x 역할 표** — 칸별 가능/불가능/확정만 기록, 최대 11 x 13

상층만으로 실용적 가치가 충분하다. 설명서가 "각본가는 주인공이 룰 Y를 특정지을 수
없도록 여러 가능성을 남겨야" 한다고 명시한다(주인공 설명서 32p). 각본가가 실제로
신경 쓰는 건 "룰 Y 후보가 몇 개 남았나"이지 "여학생이 연인A일 확률"이 아니다.

하층도 역할 배정 전체를 만들거나 경우의 수를 세지 않는다. 살아있는 룰 조합이
공급하는 역할과 엑스트라만 열로 두고, 공개 역할·아웃사이더 제약·역할 상한 같은
확정 제약을 각 칸에 적용한다. 어느 캐릭터도 필수 역할을 맡을 수 없으면 그 역할을
요구하는 룰 조합을 배제하고, 다시 표를 줄이는 과정을 고정점까지 반복한다.

### 역할·범인 표의 관측 전파

역할표는 공개 관측에서 확실히 말할 수 있는 것만 X로 바꾼다.

- 우호 능력이 실제 거부되면 해당 캐릭터는 우호 무시 역할만 가능하다.
- 거부 가능한 능력이 해결되었으면 일반 우호 무시는 그대로 두고, 반드시 거부해야
  하는 광신도·마녀만 X다. 아웃사이더[3]·간호사[2]처럼 거부 불가 능력은 이 근거에서
  제외한다.
- 불온한 소문이 모든 룰 후보에서 빠졌을 때만 장소 음모 증가 관측의 위치 교집합을
  흑막 후보에 반영한다. 교집합이 한 명이면 흑막 확정이다.
- 선택 능력의 미발동은 어떤 경우에도 X 근거로 쓰지 않는다.

범인표는 시나리오의 사건 발생 건을 열로 두고 캐릭터별 가능/불가능/확정을 기록한다.
사건 판정 직전 공개 상태를 `incidentJudged.publicContext`에 저장해야 과거 루프도 정확히
판정할 수 있다. 구 저장처럼 스냅샷이 없으면 X를 만들지 않는다.

- 발생 시 사망·부재 또는 불안 미달이면 X. AI는 모든 카운터를 불안으로 취급하므로
  불안 수치만 보고 배제하지 않는다.
- 미발생 시 생존하고 불안이 한계 이상이면 X. 하수인은 발생을 막을 수 있으므로
  이 판정에서 제외한다. 검은 고양이는 효과만 없어질 뿐 발생은 하므로 일반 판정한다.
- 범인 공개와 발생한 자살의 사건 본체 직접 사망자는 확정 근거다. P7 뒤 강제 반응까지
  합친 구 저장의 사망 목록은 자살 범인 확정에 쓰지 않는다. 한 범인이 확정되면 다른
  사건에서는 X이며, 유일 후보 확정도 한 건씩 적용해 동일 인물이 두 사건에 동시에
  확정되지 않게 한다.
- `incidentJudged.publicChanges`는 `ON_DEATH` 반응 전 사건 본체의 공개 변화만 저장한다.
  행방불명의 이동 캐릭터는 범인으로 확정하고, 제자리 이동은 음모가 증가한 장소로
  후보를 좁힌다. 살인 사건은 직접 사망자의 관측 시점 장소, 나비의 날갯짓은 직접
  카운터가 증가한 캐릭터의 관측 시점 장소로 후보를 좁힌다.
- 원격 살인·불안 확대·유포·병원 사건·사악한 기운의 오염은 효과 대상이나 장소가
  범인 위치와 무관하므로 흔적 제약을 만들지 않는다. 검은 고양이의 효과 없음과
  하수인의 사건 억제에도 공개 변화가 저장되지 않아 같은 원칙을 유지한다.

두 표는 같은 공개 관측·사건 사망 기록을 다시 계산하는 방식으로 연결한다. 역할과
범인 자체 사이에는 원문상 직접 제약이 없으므로, 역할 행이 모순으로 비었다는 이유만으로
범인 칸을 X로 만드는 식의 추가 규칙은 두지 않는다. 모순은 추가 배제 근거가 아니라
검토 신호다.

각본가 화면은 모바일에서 캐릭터별 가능한 후보 목록을 먼저 보여주고, 확정과 축소를
별도 강조한다. 전체 역할·범인 격자는 중첩된 펼침 영역에서만 보이며 격자 자체가 가로로
스크롤된다. 문서 전체에는 가로 스크롤이 생기지 않아야 한다.

---

## 6. 파일 구성

```
tragedy-looper-mastermind/
├─ AGENTS.md / CLAUDE.md      에이전트 상시 가드레일 (내용 동일, 도구별 이름)
├─ PROMPTS.md                 작업 단위 지시서
├─ SETUP.md                   코덱스 앱 사용법
├─ HANDOFF.md                 이 문서
├─ TODO.md                    구현 체크리스트 (훅 34개)
├─ gen.py                     스캐폴딩 생성기 — 훅이 실행을 차단함
├─ .claude/
│  ├─ settings.json           훅 등록
│  ├─ hooks/guard.py          gen.py·픽스처·생성물 수정 차단
│  ├─ hooks/post-check.py     규약 위반 경고
│  └─ skills/                 클로드 코드용 슬래시 커맨드 (코덱스는 미사용)
├─ data/
│  ├─ characters.json         36명 정적 메타데이터 + 한국어명
│  ├─ ko-terms.json           정발 용어 (repo id 로 재키잉)
│  ├─ ko-release.json         한국어 실물 박스 구성  ★ 아래 7절
│  └─ basic-tragedy-scripts.json  기본편 22편 — 테스트 픽스처
└─ src/
   ├─ types.ts                코어 타입 · 게임판 기하 · effectiveRole · resolvePlaceX
   ├─ engine/
   │  ├─ movement.ts          이동 해결 — 구현 완료
   │  └─ phases.ts            9단계 상태 머신 — 뼈대
   └─ impl/
      ├─ roles.ts             13종 / 훅 16   ─┐
      ├─ plots.ts             12종 / 훅  8    ├ when/effect 가 구멍
      └─ incidents.ts          9종 / 훅 10   ─┘
```

`tsc --strict` 통과 확인함 (TypeScript 5.9).

작업 시작은 `SETUP.md` 참조. `PROMPTS.md` 0번부터.

### 생성기 재실행

이 파일은 코덱스가 실행하면 안 된다(AGENTS.md 참조).

`gen.py`는 포크 레포 `data/*.jsonc` + 정발 용어 대조표 xlsx를 읽어
`src/impl/*.ts`와 `data/*.json`을 다시 만든다. 경로 상수만 고치면 된다.

> ⚠️ 재생성은 `when`/`effect`를 빈 스텁으로 되돌린다.
> 구현을 시작한 뒤에는 생성기를 돌리지 말거나, 구현부를 별도 파일로 분리할 것.

### 스캐폴딩이 담고 있는 것

각 훅에 원본 영문이 `source`로 박혀 있다. 구현할 때 이걸 술어와 효과로 옮기면 된다.

```ts
killer: {
  ko: "살인 청부업자",
  goodwillRefusal: "Optional",
  hooks: [{
    phase: "P9_ROUND_END",
    kind: "optional",
    source: {
      timing: "Day End",
      prerequisite: `The :keyPerson: has at least 2 :intrigue: and is in this character's location`,
      description: `Kill the :keyPerson:`,
    },
    when:   (_s, _self) => false,                      // ← 여기
    effect: (_s, _self) => { throw new Error(...) },   // ← 여기
  }],
}
```

`:intrigue:` `:keyPerson:` 같은 토큰은 원본 데이터의 아이콘 치환 플레이스홀더다.

---

## 7. 한국어 대응 — 주의할 점

### 레포의 세트 구분과 한국어 실물 박스가 다르다

주인공 설명서 16p는 본판을 「캐릭터 카드 (24장)」이라 명시한다.
그런데 레포 `base-game`은 18명이고, 나머지 6명이 `cosmic-evil`·`midnight-circle`로
흩어져 있다.

| id | 한국어 | repo edition | 한국어 실물 |
|---|---|---|---|
| teacher | 교사 | Cosmic Evil | **본판** |
| transferStudent | 전학생 | Cosmic Evil | **본판** |
| soldier | 군인 | Cosmic Evil | **본판** |
| blackCat | 검은 고양이 | Cosmic Evil | **본판** |
| forensicSpecialist | 감식관 | Midnight Circle | **본판** |
| ai | AI | Midnight Circle | **본판** |
| scientist | 학자 | Midnight Circle | 프로모 |
| illusion | 환상 | Midnight Circle | 프로모 |

교사·군인·감식관은 기본 추리 참조표에 실려 있고, 전학생은 FAQ Q5·Q6,
검은 고양이는 Q14에 나온다. 학자·환상은 41p가 프로모션 카드라고 직접 명시한다.

**`edition` 필드로 필터링하면 안 된다.** `data/ko-release.json`이 한국어 기준 축이다.

> ☐ **검수 필요**: 24장 구성은 설명서와 참조표 대조로 도출한 것이다.
> 실물 박스에 검은 고양이와 AI가 들어 있는지 확인할 것.

### 정발 용어 대조표 상태

`tragedy-looper-정발용어-대조표-v4.xlsx` 기준, 레포 데이터와 조인한 결과:

```
역할     88 / 88   완전 일치
캐릭터   36 / 35   한국어 번역 35건 확인, crusader 1건 비어 있음
사건     81 / 77   시트 누락 4  → Sacrifice, Diagonal Destruction ×3
플롯    127 / 125  시트 누락 2  → Infestation, Metamorphosis
```

캐릭터의 한국 발매 여부와 한국어 번역 존재 여부는 별개 축이다. 한국 미발매 캐릭터
10명 중에도 `crusader`를 제외한 9명은 포크 저장소의 `translations/ko.jsonc`에
한국어 번역이 있다. 번역이 있다는 사실을 정발 또는 한국 발매의 근거로 쓰지는 않는다.
`data/ko-release.json`은 실물 구성과 이 번역 존재 여부를 분리해 기록한다.

> ☐ **확인 필요**: `Sacrifice`는 `haunted-stage` 소속이다. 헌티드 스테이지는
> 한국어 발매 세트이므로 진짜 구멍일 수 있다. 나머지 5건은 미발매라 영어 폴백 가능.

### 조인 키를 `name`에서 `id`로

대조표는 영문 `name`으로 키잉되어 있는데 깨지기 쉽다.
예: `richStudent`의 영문명은 `Rich Man's Daughter`이고 아포스트로피가 ASCII `'`가
아니라 U+2019 `'`다. 엑셀↔CSV 왕복이나 upstream 오타 수정에서 조용히 매칭이 깨진다.

`data/ko-terms.json`은 이미 repo id로 재키잉해 두었다.
대조표 자체에도 `id` 컬럼을 추가하는 편이 안전하다.

### 번역 작업과의 관계

각 세트의 `data/*/translation.ko.jsonc`는 빈 스켈레톤이지만, 포크 저장소 루트의
`translations/ko.jsonc`가 실제 한국어 번역 사전이다. 여기서 확인한 번역과 정발
용어는 출처 등급을 구분한 채 로케일 작업에 들어간다. 두 작업은 같은 자원을 쓰지만,
번역이 있다는 이유만으로 한국 발매나 정발 용어라고 판정하지 않는다.

능력 **본문**(우호 능력 설명, 역할 추가 능력, 사건 효과)은 대조표에 없다.
다만 0~2단계에서는 능력을 코드로 구현하므로 급하지 않고, 화면에 띄울 한국어 문장은
추리 참조표 PDF에서 그대로 가져올 수 있다.

---

## 8. 첫 마일스톤

기본편 시나리오 하나를 끝까지 굴린다. `data/basic-tragedy-scripts.json`에 22편이 있다.

권장 순서:

1. `data/characters.json`을 로드해 `startLocationOf` 주입 (`types.ts` 말미의 declare)
2. `LoopState` 초기화 + 캐릭터 배치
3. `TODO.md`의 역할 훅 16개부터 채우기 — 사건·룰보다 빈도가 높다
4. `engine/phases.ts`의 P4(행동 해결)와 P7(사건) 본문
5. UI: 4분할 게임판 + 현재 단계 + 소진 카드 목록 + 각본가 전용 오버레이

5번의 각본가 전용 오버레이가 0단계인데도 즉시 값어치가 있다.
"오늘 살인 사건 예정 / 범인 여학생 / 불안 3/3 → **발생함**"을 자동으로 띄우면
실수가 사라진다.

---

## 9. 열려 있는 설계 문제

- **시그널링**: 각본가가 먼저 뒷면으로 놓고 주인공이 그 위치를 보고 놓는다.
  즉 배치 위치 자체가 정보이고, 규칙이 블러핑을 명시적으로 허용한다
  (효과 없는 카드를 장소에 놓기 — 주인공 설명서 21p). 단순 minimax가 아니라
  시그널링 게임이다. 5단계 과제.
- **평가 함수**: 패배 조건 거리와 정보 누출을 숨은 가중합으로 합치지 않는다. 축별
  독립 minimax를 기본으로 하고, 사전식 순서는 사용자가 우선순위를 선언한 경우만 쓴다.
- **최후의 싸움**: 상태 추적기 입장에선 체크리스트라 로직이 없지만,
  5단계 탐색에서는 종단 평가값이 된다.
- **UI 자동 테스트**: UI 이벤트 → 엔진 호출 경로에 자동 테스트가 없다. 현재는
  수동 플레이로 검증한다.

### 우호 능력 구조 확장

아래 세 능력은 `goodwill.ts`에 분기만 추가해서 끝나지 않는다. 선언·상태·UI
스키마를 먼저 확장해야 하며, 임의로 값을 고르지 말아야 한다.

- `forensicSpecialist [우호2]`: 두 캐릭터와 이동할 카운터를 함께 받는 복수 대상
  선언이 필요하다.
- `scientist [우호3]`: 프로모 구조화 데이터와 특수 게이지 증감 선택이 필요하다.
- `illusion [우호3]`: 대상 캐릭터와 이동할 장소를 함께 받는 선언이 필요하다.

---

## 미완료 작업

이 절은 C·D·E 착수 전 기준 목록이다. 아래 항목은 이번 정리에서 구현하지 않는다.
우선순위는 `P0`(C·D·E 전 검증 또는 외부 확인 즉시 처리), `P1`(다음 구현 후보),
`P2`(선행 설계·근거 확보 뒤), `P3`(장기 로드맵) 순이다.

### 중단되고 돌아오지 않은 것

| 우선순위 | 항목 | 착수 조건 |
|---|---|---|
| P1 | P6 자동 스킵 원인 추적 | 같은 현상이 재발하면 진행을 계속하거나 저장 데이터를 삭제하기 전에 시나리오·난이도, 루프·날짜·단계, 저장 상태, 직전 진행 기록, 콘솔 오류를 덤프한다. 재현 또는 덤프 없이는 추측해 고치지 않는다. |
| P1 | 학생 회장 문제의 근본 원인 추적 | 저장 데이터 삭제로 증상이 사라진 상태다. 재발 시 삭제 전에 위와 같은 상태 덤프와 학생 회장 관련 카드·카운터·능력 사용 이력을 확보한 뒤 착수한다. |
| P1 | 연쇄 살인마 관련성 축소 | 역할표 후보로 원인 캐릭터를 안전하게 좁혀도 실제 결과를 누락하지 않는다는 계약을 먼저 정의하고, 기존 축별 관련성 전수 대조 하네스에서 증명할 수 있을 때 착수한다. |
| P2 | 광신도 미지원 범위 조사 | 지원 대상 참극 세트와 전체 각본 목록을 고정한 뒤 광신도가 등장해 현재 관련성 투영에서 제외되는 편 수, 필요한 선택 모델, 구현 비용을 조사한다. 조사 전용이며 규칙 원문 없이 구현하지 않는다. |
| P1 | P5~P9 분산 측정 | 패배 위험 경로 재정의 검증과 축별 관련성 계약을 마친 뒤, 각 단계의 합법 전이를 실제 엔진 경로로 열거할 수 있을 때 착수한다. |
| P2 | 입문편 시나리오 출처 분류 확정 | 7편 중 설명서 수록 2편 외 5편은 현재 소거법으로 팬 제작 분류돼 있다. 실물 각본·설명서 또는 직접 출처를 대조할 수 있을 때만 확정하고, 그전에는 현재 근거와 확신도를 유지한다. |

### 기록만 하고 미착수

| 우선순위 | 항목 | 착수 조건 |
|---|---|---|
| P2 | 미사용 역할·룰 조회 | 시나리오 편집 도구의 레드 헤링 조회 요구사항과 읽기 전용 범위를 확정한 뒤 착수한다. 현재 시나리오의 진실 정보와 섞지 않는다. |
| P2 | 시나리오 제목 정발 대조 | 한국어 각본가 설명서의 해당 페이지나 실물 각본처럼 제목 출처를 직접 대조할 수 있을 때 착수한다. 확인 전에는 임의 번역하지 않는다. |
| P2 | 진행 기록 필터(캐릭터·장소별) | 기존 진행 기록의 캐릭터·장소 참조가 구조화돼 있는지 감사하고, 문자열 기록의 구조화 마이그레이션 경계를 정한 뒤 착수한다. |
| P2 | 역할 배정 층 CSP | 현재 칸 독립 고정점 모델로 설명할 수 없는 실제 모순 또는 시나리오 편집 검증 요구를 사례로 확보하고, 완전 배정 열거 없이 필요한 제약과 성능 상한을 명세한 뒤 착수한다. |
| P2 | 확장 괴뢰의 우호 무시 P5 사용 | 현재 미지원이다. 확장 지원 시 우호 무시에 더해 우호 능력 사용 조건이 충족된 능력을 각본가 능력 단계에 쓰는 선택 모델과, 이세계인 사망 효과로 P5 즉시 종료가 생기는 종료 경로를 원문과 함께 명세한 뒤 착수한다. 기본편 공개 패배 추론에는 P5 즉시 종료 후보를 넣지 않는다. |
| P1 | 환상 [우호3] 임의 장소 이동 | 대상 캐릭터와 목적지를 함께 선언하는 선택 스키마, 합법 장소 검증, 이동 해결 순서를 명세한 뒤 착수한다. |
| P1 | 감식관 [우호2] 대상 2명 선택 | 두 캐릭터와 이전할 카운터를 원자적으로 선언하는 복수 대상 스키마와 실패 시 롤백 계약을 명세한 뒤 착수한다. |
| P1 | 학자 [우호3] 특수 게이지 | 특수 게이지의 상태 위치, 초기화 시점, 증감 선택, 저장 호환 계약을 원문으로 확정한 뒤 착수한다. |

### 사용자 검수 대기

| 우선순위 | 항목 | 착수 조건 |
|---|---|---|
| P0 | 한국어 본판 캐릭터 카드 24장 구성 | 사용자가 실물 박스에서 검은 고양이와 AI 포함 여부를 확인하면 `data/ko-release.json`의 본판 구성을 확정한다. 확인 전에는 현재 설명서·참조표 기반 판정을 유지한다. |
| P0 | `Sacrifice` 사건 한국어명 | 헌티드 스테이지 실물 또는 신뢰할 수 있는 한국어 자료로 확인한다. 현 정발 대조표 누락 상태에서는 임의 번역하지 않는다. |
| P0 | `crusader` 한국어명 | 사용자가 번역 출처나 채택할 명칭을 확인하면 반영한다. 한국 미발매라는 이유만으로 번역 부재를 일반화하거나 임의 번역하지 않는다. |

### 미검증

| 우선순위 | 항목 | 착수 조건 |
|---|---|---|
| P0 | 최근 변경사항의 실전 플레이 확인 | 현재 변경 묶음의 대표 시나리오와 확인 체크리스트를 고정하고, 실제 UI로 한 루프 이상 진행할 수 있는 환경에서 검증한다. 단위 훅 직접 호출만으로 완료 처리하지 않는다. |
| P0 | 패배 위험 경로 재정의 | 대표 공식 각본의 성립 직전·성립·미성립·차단 경로를 실제 UI와 엔진 종단 판정으로 대조한 뒤 C·D·E의 계산 입력으로 사용한다. |
| P0 | 지침 생성 A·B | 공식 8편의 실제 플레이 관점 검수와 390×844 화면 전달성을 확인하고, A의 우선순위 차이와 B의 회수 근거가 사용자 기대와 맞는지 승인받은 뒤 C·D·E를 착수한다. |

### 전체 캐릭터 지원 — 시나리오 편집 도구를 향한 별도 로드맵

`servant`와 `sacredTree` 지원을 완료해 현재 미지원 캐릭터는 8명이다. 한국 발매 여부와 번역 존재
여부는 별개다. `crusader`를 제외한 미지원 7명과 지원된 두 캐릭터는 포크 저장소
`translations/ko.jsonc`에 한국어 번역이 있고, `data/ko-release.json`도 두 축을
분리한다. 최종 목표는 이 캐릭터들을 선택·검증할 수 있는 **시나리오 편집 도구**이며,
캐릭터만 화면에 추가하는 것으로 완료하지 않는다.

| 단계 | 우선순위 | 범위 | 착수 조건 | 시나리오 편집 도구 산출물 |
|---|---|---|---|---|
| 완료 | P1 | `servant` | 강제 동행, 서로 다른 목적지가 둘 이상일 때만 리더 선택, 대신 사망, 우호 4 추가 주인을 루프 상태·저장·공개 관측에 반영했다. 메이드 자신이 불사인 경우는 `QUESTIONS.md`의 보수적 임시 판정을 따른다. | 이동 분기 선택과 루프 한정 관계를 저장 가능한 상태로 구조화 |
| 완료 | P1 | `sacredTree` | 리더 선택은 P4 행동 해결 직후의 별도 입력으로 두고, P5는 유효 역할이 우호 무시일 때 건너뛸 수 없는 강제 선택으로 구현했다. 정확한 리더 시점과 보호 카운터 포함 여부는 `QUESTIONS.md`의 보수적 임시 판정을 따른다. | 양측의 공통 카운터 이전 계약, P5 조건 스냅샷, 우호 무시 계열 공개 추론을 저장 가능한 상태로 구조화 |
| 완료 | P1 | 커뮤니티 시나리오 `community:naughty-cat` | 「못된 고양이」를 생성 데이터 밖 `scenarios/` 번들과 별도 출처 오버레이에 보존했다. 각본가 우호 금지 사용 불가는 손패 소진이 아니라 공통 배치 합법성 제한이다. | 편집기와 같은 원시 시나리오 스키마, 안정 ID, 제작자·원문 지침·승리 조건 메타데이터, 구조화 특수 규칙 |
| 2 | P2 | `youngGirl`, `sectFounder`, `littleSister` | 세 캐릭터의 `source`와 현재 상태·우호·특성 계약을 대조해 기존 구조만으로 표현 가능함을 확인하고, 편집기의 캐스트·시작 장소·역할 검증 입력 형식을 먼저 정한다. | 기존 구조 안에서 캐릭터 선택, 필수 입력, 시나리오 검증까지 가능한 첫 편집 경로 |
| 3 | P2 | `copycat` | 역할 복사에 따른 역할 상한·최후의 싸움·가설 공간 계약을 확정한다. | 복제 역할을 저장·검증·추론할 수 있는 확장 편집 스키마 |
| 4 | P3 | `metaworldDenizen`, `crusader`, `partTimer`, `partTimerAlternate` | 해당 참극 세트 지원을 먼저 완료하고, hope/despair 카운터, supernatural 태그, 캐릭터 교체의 엔진·저장·UI 계약을 확정한다. `crusader` 한국어명은 사용자 검수 전 임의 결정하지 않는다. | 지원 참극 세트 안에서 전체 캐릭터와 교체·특수 상태를 검증하는 완성형 시나리오 편집 도구 |

---

## 작업 로그

커밋 이력은 `git log --oneline`을 보라. 여기에는 **커밋 메시지로는
알 수 없는 것**만 적는다 — 결정의 근거, 검토했으나 채택하지 않은 안,
남은 우려사항.

### 2026-08-27 커뮤니티 시나리오 번들과 특수 규칙

- `data/*.json` 생성물은 수정하지 않고 `scenarios/community-scripts.json`과
  `scenarios/scenario-source.json`을 카탈로그에 합성한다. 이는 향후 편집기 저장값을
  생성 데이터와 분리하면서도 번들 시나리오와 같은 어댑터·검증 경로를 쓰기 위한 경계다.
- 「못된 고양이」의 우호 금지 금지는 카드를 소진시키거나 손패 정의에서 제거하지 않는다.
  손패에는 `특수 규칙`이라는 짧은 사유와 함께 비활성으로 남고, 전문은 룰과 역할
  패널의 시나리오 특수 규칙에서 확인한다. `validatePlacement()`는 UI·탐색·직접 호출을
  모두 거부한다.
- 메이드의 복수 시작 장소는 하수인처럼 매 루프 고르는 값이 아니라 시나리오 작성 시
  확정하는 값이다. 「못된 고양이」는 사용자 확인에 따라 학교를
  `scenario.scriptSpecified["startLocation:servant"]`로 보존한다. 복수 후보를 가진
  비-하수인 캐릭터의 고정값이 빠지면 `validateScenario()`가 거부한다.
- A~E 원문 대조는 `tools/phase5-feasibility/results/naughty-cat-guidance-audit.md`에
  보존한다. 정적 지침이 회수하지 못하는 루프별 공개 순서는 별도 후속 범위다.

### 2026-08-27 「못된 고양이」 P4 진행 불가 복구

- 메이드의 주인이 이동하는 P4에서 광신도 무시 범위 미리보기가 메이드 동행 선택보다
  먼저 복제 상태의 이동을 해결해 `servant movement choice is required`를 던졌다. 조작
  패널 열기는 기존 원자적 단계 처리 밖의 단순 렌더 경로여서 오류가 롤백·기록되지 않고
  브라우저 콘솔에만 남았다.
- 광신도 미리보기는 메이드 선택 전 계산을 보류하고, 선택 뒤에만 이동 후 범위를
  계산한다. 신수의 카운터가 0이면 이전 조건은 불충족이므로 P4/P5 선택을 요구하지 않는다.
- P2는 각본가 카드 3장, P3는 각본가 3장과 각 주인공 1장씩이 없으면 엔진 단계 전환을
  거부한다. 이미 불완전한 P4 저장 상태는 효과 적용 전이라면 `배치 단계로 돌아가기`로
  P3에 복귀해 기존 카드를 확인·보충할 수 있다.
- `window error`와 `unhandledrejection`도 최근 오류에 저장한다. 정상 렌더가 중단돼도
  상태 복사, P4 배치 복구(가능한 경우), 새 게임 진입 버튼을 고정 복구 안내로 삽입한다.
- 상태 덤프에는 시나리오 역할 문맥, `placed`, `pendingLoopEnd`의 명시적 `null`, 즉시 종료
  대기 키, 최근 오류, P1~P9의 완료/현재 차단/대기 상태와 현재 차단 사유를 포함한다.

### 2026-08-28 메이드 강제 동행 정정

- `Observer's Handbook` 7쪽 원문은 주인이 메이드의 장소에서 `would move`하면 메이드가
  그 캐릭터와 함께 이동한다고 먼저 규정하고, 리더 선택은 괄호 안의 `multiple ways`에만
  한정한다. 따라서 동행 거부는 없으며 서로 다른 목적지가 둘 이상일 때만 리더가 고른다.
- 이동 합성·이동 금지·금지 장소를 먼저 판정해 실제로 이동하는 동소 주인만 동행 원인으로
  삼는다. 목적지가 하나면 주인이 여러 명이어도 메이드는 자동으로 그곳을 따른다. 목적지가
  갈릴 때만 목적지별 대표 주인을 선택지로 표시한다. 우호 4로 추가한 주인도 같은 집합에
  포함한다.
- FAQ Q25에 따라 강제 동행은 메이드 자신의 이동 카드와 이동 금지를 덮어쓴다. 이동하는
  주인이 없으면 메이드 자신의 일반 이동을 그대로 해결한다.
- 영문 `would move from this character's location`은 금지 판정 뒤 실제 이동 결과를 조건으로
  삼는 현재 순서와 일치하므로 `QUESTIONS.md`에 새 질문을 추가하지 않았다. 구 저장 상태의
  폐기된 `servantMovementChoice: "decline"`은 불러올 때 제거한다.

### 2026-07-31 초기화와 행동 해결의 경계

- 시작 장소 후보가 하나면 자동 배치하지만, 여러 개면 임의로 고르지 않고
  `scenario.scriptSpecified["startLocation:<character>"]`를 요구한다.
- `comesInLater` 캐릭터는 현재 시작 시점에 배치한다. 실제 지연 등장은 별도
  상태 설계 전까지 확장하지 않는다.
- 이동은 기존 `movement.ts`의 `resolveMove()`를 호출한다. 겹침 합성 뒤
  이동 금지와 금지 장소를 판정하며, 행동 해결에서는 이동을 다른 효과보다 먼저
  처리한다.
- 음모 금지는 카드별 판정이 아니라 라운드 전체 주인공 카드를 집계한다.
- 배치 검증은 같은 진영의 동일 대상 중복, 사망 대상, 소진 카드 재사용을 막는다.
  각본가와 주인공의 카드 중첩 및 효과 없는 장소 카드 블러프는 허용한다.

### 2026-07-31 역할 판정과 강제 훅 동시성

- 역할은 `scenario.cast`를 직접 읽지 않고 `effectiveRole()`로 판정한다.
  망상 확대 바이러스의 역할 변이를 한 진입점에 모으기 위한 결정이다.
- 강제 훅은 모든 대상을 먼저 확정한 뒤 효과를 일괄 적용한다. 연쇄 살인마 둘이
  서로만 남은 경우 양쪽 모두 사망해야 하므로 순차 적용안은 채택하지 않았다.
- 광신도는 자신이 있는 장소와 그 장소의 캐릭터에 놓인 음모 금지를 모두 무시한다.
  장소 대상은 제외한다는 해석은 한국어 규칙 문구와 맞지 않아 배제했다.
- 선동가는 자신도 대상으로 삼을 수 있고, 연쇄 살인마는 자신을 제외한 생존
  캐릭터가 정확히 한 명일 때만 발동한다.

### 2026-08-01 친구와 연인의 루프 간 정보

- `lover`는 연인B, `lovedOne`은 연인A이며 연인A만 주인공 사망 능력을 가진다.
- 동시 사망 처리 중 이미 죽은 캐릭터에는 추가 카운터를 놓지 않는다.
- 친구의 역할 공개 이력은 다음 루프에도 필요하므로 루프 종료 스냅샷을
  `GameState.history`에 보존한다.

### 2026-08-02 시간 여행자와 변수의 능력 분리

- 캐릭터 사망은 시간 여행자 불사 판정 뒤 보호 카운터를 확인한다. 불사로
  사망하지 않으면 보호 카운터도 제거하지 않는다.
- 시간 여행자의 마지막 날 선택 능력은 사망이 아니라 주인공 패배다.
  주인공 사망 차단 효과와 같은 경로로 처리하지 않는다.
- 변수는 역할 자체가 바뀌지 않는다. `effectiveRole()`은 계속 `factor`를
  반환하고, `effectiveAbilityRoles()`가 장소 음모에 따라 선동가나 핵심 인물
  능력만 추가한다.
- 계약의 서명 패배 조건은 핵심 인물 능력 보유자가 아니라 실제 핵심 인물 역할만
  본다.

### 2026-08-02 사건의 발생과 효과를 분리

- 사건 발생은 범인의 생존과 불안 한계 충족을 함께 요구하며 선택권이 없다.
- 결과는 `fired`와 `effectApplied`를 분리한다. 효과가 없어도 발생했다면
  발생 이력을 남긴다.
- 병원 사건의 영문 파생 데이터에는 두 번째 설명이 비어 있었다. 원문 누락으로
  판단해 병원 음모 1개 이상이면 병원 캐릭터 사망, 2개 이상이면 주인공 사망으로
  처리한다.
- 검은 고양이는 사건 발생을 막지 않고 효과만 없앤다. 따라서 발생 이력을
  참조하는 미래 변경 계획에는 영향을 준다.

### 2026-08-03 우호 능력 해결 모델

- 우호 능력은 랭크, 위치 제한, 1루프당 횟수, 각본가의 해결/거부를 한 경로에서
  검증한다. 사용해도 우호 카운터는 줄지 않고, 거부돼도 횟수 제한 능력은 소진된다.
- 같은 P6 안에서는 선언 순서대로 해결해 앞선 효과가 다음 능력의 조건에 반영된다.
- 아웃사이더 [우호3]과 간호사 [우호2]의 거부 불가 필드가 파생 과정에서
  누락됐다. 사용자 승인 아래 `data/characters.json`을 수동 보정한 예외이며,
  `gen.py`를 다시 실행하면 사라지는 위험이 남아 있다.
- `minLoop`는 능력 사용 가능 루프이고 `comesInLater`는 캐릭터 등장 시점이다.
  둘을 같은 상태로 합치지 않는다.

### 2026-08-03 패배 조건과 잔여 거리

- 패배 조건은 역할 훅을 진실의 원천으로 두고 `loss.ts`가 UI용 거리로 수집한다.
- 선택형 조건은 “성립”과 “실제 발동”을 분리하며, 선택 기록은 라운드 종료 뒤
  초기화한다.
- 주인공 사망은 별도 범주로 두고 살인 청부업자, 연인A, 병원 사건만
  `attemptProtagonistDeath()`를 통과시킨다. 시간 여행자·핵심 인물·친구와
  룰 패배는 군인의 사망 방지 대상이 아니다.
- 거대 시한폭탄의 장소 X는 고정값이 아니라 `resolvePlaceX()`로 계산한다.

### 2026-08-03 룰 훅의 계층 분리

- 불온한 소문은 장소 선택이 필요하므로 엔진이 임의 장소를 고르지 않는다.
- 인과율은 직전 `GameState.history` 스냅샷을 읽는다.
- 계약의 서명 시나리오 구성 제약은 런타임 훅이 아니라
  `validateScenario()`에서 검증한다.
- 봉인된 것, 계약의 서명 패배, 미래 변경 계획, 거대 시한폭탄은 `loss.ts`,
  망상 확대 바이러스는 `effectiveRole()`에 구현한다. 원문 보존 훅은
  `IMPLEMENTED_ELSEWHERE` no-op으로 남기며 중복 구현하지 않는다.

### 2026-08-03 로컬 UI 입력 경로

- 단일 사용자 Vite UI와 `localStorage` 기반 시나리오·루프 스냅샷을 사용한다.
- P2 각본가 배치, P3 주인공 배치, P4 공개·해결을 카드 기반 주 입력 경로로 둔다.
- P6 우호 UI는 구조화 데이터에서 대상, 선택지, 위치, 소진 상태를 계산한다.
  같은 랭크 능력은 `abilityIndex`로 구분한다.
- 세력권과 복수 대상처럼 현재 상태로 판정할 수 없는 능력은 추측하지 않고
  비활성화한다.
- UI 이벤트에서 엔진 호출까지의 자동 통합 테스트는 없다. 다음 세션은 수동
  플레이로 이 경로를 검증한다.

### 2026-08-05 저장 데이터 정책

- 저장 키는 버전 접미사 없는 `tragedy-looper-mastermind:tracker` 하나만 쓴다.
  구 버전 데이터는 읽지 않으며 명시된 옛 키만 삭제한다.
- 저장 상태는 현재 시나리오로 만든 기본 객체와 재귀 병합한다. 새 필드는 별도
  버전 마이그레이션 대신 기본값으로 보충한다.
- 타입 충돌, 알 수 없는 시나리오, JSON 손상은 부분 복원하지 않는다. 현재 저장
  전체를 삭제하고 새로 시작한다는 알림을 표시한다.
- 헤더의 앱 버전은 저장 스키마 버전이 아니라 `package.json`의 빌드 버전이다.
- 새 게임은 현재 활성 게임만 폐기하고 `activeScenarioId`를 비운 뒤 시나리오 선택
  화면으로 돌아간다. 선택한 시나리오는 과거 저장이 있더라도 새 상태로 덮어쓴다.
- 완전 삭제는 `tragedy-looper-mastermind:` 접두사 키만 열거해 삭제한다.
  같은 출처에 있을 수 있는 `tragedy-looper-ko:` 데이터는 건드리지 않는다.
- 초기화 동작은 평소 접힌 하단 정보 영역에 두고, 게임 종료 화면에는 자주 쓰는
  새 게임 동선만 추가한다. 두 동작 모두 실행 전에 브라우저 확인을 거친다.

### 2026-08-24 localStorage 용량 상한과 실패 정책

- `observationsByLoop`는 복원·추론 입력이 아니라 헤더의 저장 시점 개수를 위한
  메타데이터다. 매 저장마다 `LoopState` 전체를 복제하면 누적 `phaseLog`가 다시
  복제되어 저장 크기가 제곱으로 증가하므로, 시각·이유·8자리 상태 지문만 남긴다.
- 루프 종료 `GameState.history`, `phaseLog`, `publicInformationThisLoop`는 규칙과
  공개 추론에 필요하므로 축약하지 않는다. 역할표·사건 범인표·가설 공간은 현재
  상태와 공개 관측에서 재계산하며 저장하지 않는다.
- 공개 관측 payload를 매 단계에 넣은 상한 회귀에서 4루프×7일은 약 6.64M자에서
  0.283M자로, 번들 최장 5루프×7일은 약 8.30M자에서 0.342M자로 줄었다.
  구 관측의 전체 상태는 로드 시 지문 메타데이터로 변환하고 다음 저장에서 제거한다.
- `localStorage.setItem()` 실패는 게임 상태 변경의 실패가 아니다. 메모리 상태와
  진행 UI를 유지하고, 새로고침 복원이 되지 않는다는 지속 경고를 표시한다.
- 역할 제약의 원인이 되는 라운드 사실은 `LoopState.roundEvidence`에 날짜별로
  저장한다. 전체 보드 스냅샷을 다시 복제하지 않고 원자적 사망 묶음의 단계·사망자,
  P9 직전 단둘 장소·두 캐릭터·불안 수치, 즉시 종료 시점·사유만 남긴다. 전체 사망자
  목록은 묶음에서 파생한다. 완료 루프 스냅샷에는 종료를 일으킨
  `pendingImmediateLossKeys`를 삭제 전에 보존한다.
- 이 기록을 포함한 용량 회귀에서 4루프×7일은 282,715자에서 286,375자로
  3,660자(1.29%), 최장 5루프×7일은 341,780자에서 346,214자로
  4,434자(1.30%) 증가했다. 각각의 `roundEvidence` 자체는 3,571자와 4,327자다.

### 2026-08-25 공개 패배 관측의 종료 경로

- 공개 패배 원인은 최종 보드 상태만으로 판정하지 않고 종료 사유·날짜·단계를 함께
  본다. 기본편의 즉시 종료 후보 단계는 P6·P7·P9이며 P4·P5는 후보가 아니다.
- P6·P7·P9 `effect` 종료는 그 종료 단계에서 죽은 핵심 인물 능력 보유자만 후보로
  삼는다. 같은 날 앞선 단계의 사망자는 후보에 섞지 않는다. 마지막 날 P9 즉시
  종료에만 시간 여행자를 함께 남긴다.
- 마지막 날 P9를 모두 지나 `lastDay`로 종료된 경우에는 Loop End 룰과 친구만
  후보로 삼고, 이미 발동하지 않은 핵심 인물·변수·시간 여행자는 제외한다.
- `protagonistDeath`는 일반 `effect`와 분리한다. P7은 공개된 병원 사건, P9는
  살인 청부업자·연인A 조건만 설명 후보가 된다.
- 확장 괴뢰가 지원되면 P5 이세계인 사망 경로가 추가될 수 있으나 현재는 미지원이라
  위 기본편 후보에 넣지 않는다.

### 2026-08-03 UI 재설계 보류

- 수동 수정은 상시 버튼이 아니라 캐릭터·장소 모달로 모으고, 해당 라운드 동안
  수동 변경 표시를 남기는 안이 승인됐다.
- 공개 전 카드 회수와 공개 뒤 직전 단계 한 번 되돌리기를 분리한다.
- 실전 플레이 피드백을 먼저 받기로 해 두 기능은 아직 구현하지 않았다.

### 2026-08-03 타입 안전성

- 행동 카드 13종은 `ACTION_CARDS`를 단일 원천으로 두고 `ActionCard` 타입을
  파생한다.
- DOM 문자열은 `isActionCard()`로 검증한다. 선택 전 빈 값이 정상인 입력만
  예외적으로 미선택을 허용한다.
- 이 저장소에서는 저장소 `AGENTS.md`가 상위 기본값보다 우선하므로
  `npx tsc --noEmit --strict`를 직접 실행한다.
- `test/resolve.test.ts`의 `simultaneous-mandatory` 한 건은 기존 TODO다.

### 2026-08-03 우호 능력 오기재 수정

- 원인: `ko-translations.json`이 영문 `description`을 단독 키로 쓰는
  평면 사전이라, 문구가 같고 제약이 다른 능력끼리 충돌했다.
- 아웃사이더/회사원, 재벌가 손녀/아이돌 두 건에서 실제 오기재가 발생했다.
- `goodwill-abilities.json`의 `ko`가 사전 조회보다 우선하도록 변경했다.
- 중복 영문 문구를 자동 탐색하는 회귀 테스트를 추가했다.
- 남은 우려: 확장 세트 캐릭터 추가 시 같은 충돌이 다시 생길 수 있다.

### 2026-08-03 한국어판 26명 구조화 범위

- 한국어판 본판 24명과 프로모 2명, 랭크 우호 능력 35건을 구조화 데이터로
  관리한다. 능력이 없는 입원 환자는 빈 배열을 유지한다.
- 생성 데이터에는 학자 [우호3], 환상 [우호3·4]가 `implemented: false`로 남아
  있다. 생성물을 직접 수정하지 않고 런타임 엔진 지원 목록을 기준으로 판정하며,
  환상 [우호4]만 실제 해결을 지원한다.
- 같은 영문 문구를 가진 능력은 캐릭터별 한국어 문구가 달라야 하며, 실제로
  동일한 남학생/여학생 조합만 회귀 테스트의 허용 목록에 둔다.

### 2026-08-03 정보 공개형 우호 능력 A

- 신 [우호3]은 발생 여부와 무관하게 시나리오 사건의 범인을 공개한다.
- 형사 [우호4]는 이번 루프에 실제 발생한 정확한 사건 회차만 공개할 수 있다.
  아직 오지 않은 사건과 조건 미충족으로 지나간 사건을 모두 거부한다.
- 정보원 [우호5]은 리더가 시나리오 밖 룰 X를 선언할 수 있고, 각본가는 실제
  사용된 다른 룰 X를 공개한다.
- AI [우호3]은 발생 판정과 발생 부수효과 없이 AI를 범인으로 사건 효과만
  해결하며, 필요한 대상 선택은 리더에게 받는다. 날짜와 실제 발생 여부에 관계없이
  시나리오의 모든 사건 회차가 후보이고, 해결 날짜는 예정 날짜와 별도로 기록한다.
- 공개 결과는 이번 루프 목록에 쌓아 각본가가 전달할 내용을 확인하게 한다.
  사건별 추가 선택 필드는 9종 사건 계약으로 고정했다.

### 2026-08-03 지속 효과형 우호 능력 B

- 하수인 [우호3]은 자신이 범인인 사건을 발생 조건 단계에서 차단해 발생 이력도
  남기지 않는다. 다른 범인의 사건에는 영향을 주지 않는다.
- 하수인이 나비의 날갯짓 범인이면 미래 변경 계획은 성립하지 않는다.
  검은 고양이는 발생하되 효과만 없으므로 미래 변경 계획이 성립한다.
- 군인 [우호5]은 주인공 사망만 막고 차단자를 `blockedBy`에 남긴다.
  살인 청부업자, 연인A, 병원 사건은 막지만 시간 여행자의 선택형 패배는 막지 않는다.
- 차단 사실은 주인공에게 공개할 정보가 아니므로
  `publicInformationThisLoop`에 기록하지 않는다.

### 2026-08-04 스마트폰 우선 UI

- 주 사용 환경을 노트북이 아니라 스마트폰 세로 화면으로 정했다. 실물 플레이
  자리에는 게임판과 카드가 이미 놓여 있어 노트북을 펴기 어렵고, 화면의 값어치는
  게임판 재현보다 실물에 드러나지 않는 역할·사건·패배 조건 같은 비공개 정보
  표시와 단계 조작에 있다.
- 기본 스타일을 스마트폰 단일 열로 두는 모바일 우선 CSS로 전환했다. 데스크톱
  스타일을 기본으로 두고 `max-width` 미디어 쿼리로 덮어쓰던 구조는 폐기했다.
- 390×844 기준 게임판 높이는 기존 2,088px에서 9명 분산 391px, 11명 한 장소
  집중 561px로 줄었다. 전자는 화면 절반 이내를 충족하고, 후자는 세로 스크롤을
  허용하되 하단 조작 독은 계속 고정한다.
- 768px와 1,200px 전용 배치는 아직 시작하지 않았다. 현재 넓은 화면에서는
  스마트폰용 단일 열이 그대로 늘어나며, 전용 2열·3열 배치는 폰 실사용 검증 뒤
  결정한다.

### 2026-08-05 훅 테스트와 실제 진입점

- 훅의 `when`/`effect`를 직접 호출하는 단위 테스트는 훅 내부 로직만 보증하며,
  실제 플레이에서 그 훅이 수집·호출되는지는 보증하지 않는다.
- 연인 사망 반응은 직접 호출 테스트가 통과했지만 `killCharacter()`가 훅을
  해결하지 않아 실제 플레이에서는 동작하지 않았다. 사망자를 원자적 효과
  묶음별로 수집하고 배치가 닫힐 때 `ON_DEATH` 훅을 해결하도록 변경했다.
- 새 훅은 조건을 만족하거나 만족하지 않는 단위 테스트와 별개로,
  `killCharacter()`, `advanceGame()`, `resolveGoodwillAbility()` 같은 실제
  진입점을 통과하는 통합 테스트를 최소 하나 둔다.

### 2026-08-07 수동 교정과 즉시 종료의 분리

- 생존 상태 수동 변경은 입력 교정이므로 현재 상태와 패배 조건 거리만 바꾸고
  즉시 종료 사건을 만들지 않는다.
- 정규 사망 배치는 시작 전후의 활성 즉시 조건을 비교해 새로 성립한 키만
  `pendingImmediateLossKeys`에 기록한다. 배치 시작 전부터 성립한 수동 교정은
  이후 단계 진행이나 무관한 사망 효과에서 즉시 종료로 승격되지 않는다.
- `settleGameFlow()`는 현재 상태 전체를 다시 즉시 종료로 해석하지 않고 이 기록만
  확정한다. P9는 동시 강제 효과를 모두 해결한 뒤 `evaluateLoss()` 전체를 판정하므로
  수동 교정으로 성립한 조건도 그때 반영한다.
- 엔진 직접 호출 테스트와 별도로 실제 앱 버튼으로 P2 생사 토글, P2/P3 카드 배치,
  P4 해결, P9 판정을 통과하는 Chrome 회귀 검사를 기본 테스트에 포함한다.

### 2026-08-07 추가 루프 하우스 룰

- 공식 규칙은 각본에 정해진 루프 횟수를 마치면 최후의 싸움으로 이동하며 추가
  루프를 규정하지 않는다. 따라서 최종 루프 패배 뒤의 추가 진행은 반드시
  `추가 루프 (하우스 룰)`로 공개한다.
- 근거는 BakaFire 공식 FAQ Ru07이다. 모든 참가자에게 어떤 규칙인지 알린다면
  특수 규칙을 만들어 특정 각본에 적용해도 된다고 답한다.
  <https://bakafire.main.jp/rooper/sr_game_02_rule.htm>
- `scenario.loops`는 공식 각본 값이므로 변경하지 않는다. 실제 `LoopState.loop`만
  증가시키고 `GameState.extraLoopsPlayed`에 선택 횟수를 기록한다. 추가 루프도
  정상적인 루프 종료·이력·초기화 경로를 사용한다.
- 신은 지정 루프 이후의 추가 루프에서도 처음부터 등장한다. 전학생은 원문대로
  매 루프 되감기 때 다시 미등장 상태가 되고 지정 날짜에 등장한다. FAQ Q6의
  등장 시점 검증은 추가 진행 횟수가 아니라 원래 `scenario.loops`와
  `daysPerLoop`를 기준으로 유지한다.

### 2026-08-08 아웃사이더의 룰 비연관 역할

- `CharacterData.plotLessRole`은 `data/characters.json`의 값을 런타임까지
  보존한다. 생성물이나 `gen.py`는 변경하지 않는다.
- 아웃사이더의 배정 역할은 선택된 룰 Y 1개와 룰 X 2개의 `addsRoles` 어디에도
  없어야 하며, `validateScenario()`가 시나리오 시작 전에 거부한다.
- 아웃사이더는 엑스트라를 맡을 수 없다. 반드시 참극 세트의 역할 중 현재 룰의
  `addsRoles`에 없는 역할을 맡는다. 검증기는 엑스트라 배정과 활성 룰 역할 배정을
  서로 다른 한국어 오류로 거부한다.
- 기본편 18번 각본 `Trouble in Paradise`와 번들 밖 `Vertinari Gambit` 모음의
  한 각본은 팬 제작 시나리오이며, 아웃사이더를 엑스트라로 배정해 공식 규칙을
  위반한다. 공식 시나리오의 위반은 0건이다. upstream 보고 대상이 아니며,
  역할을 임의로 고치지 않고 UI에서 이유를 경고한 뒤 시작을 막는다.
- `data/scenario-source.json`은 생성된 각본 JSON을 수정하지 않고 출처를 덧씌운다.
  각본가 설명서의 공식 10편은 First Steps 2편(`The First Script`,
  `Prevailing Secrecy`)과 현재 번들의 Basic Tragedy 8편으로 전부 식별됐다.
  따라서 현재 번들의 나머지 14편은 소거법으로 `community`로 분류하고,
  `unknown`은 비워 둔다. 이 community 분류는 실물 설명서 최종 대조 전까지의
  판정이며, 근거와 확신도를 오버레이의 `_basis`에 함께 기록한다.
- 공식 각본 검증 실패는 테스트 실패로 취급한다. 팬 제작 각본의 검증 실패는
  카탈로그에 유지하되 선택 화면에서 출처·위반 이유와 시작 불가 상태를 표시한다.
- 이 특성은 시나리오 선택 화면, 플레이 중 캐릭터 카드·상세 화면, 각본가의
  룰과 역할 목록에 표시한다. 최후의 싸움은 기존 전체 역할 목록과
  `effectiveRole()` 판정을 그대로 사용한다.

## 현재 상태

다음 세션은 새 기능 구현보다 실전 플레이 검증을 우선한다.

### 구현 완료

- 0~7단계
- A: 신 [우호3], 형사 [우호4], 정보원 [우호5], AI [우호3]
- B: 하수인 [우호3], 군인 [우호5]

### 미구현 우호 능력 3개

- `forensicSpecialist [우호2]`: 캐릭터 2명과 이동할 카운터를 함께 받는 복수 대상
  선언이 없다.
- `scientist [우호3]`: 특수 게이지 상태와 증감 선택 모델이 없다.
- `illusion [우호3]`: 이동 대상 캐릭터와 목적지를 함께 받는 선언이 없다.

### 지원·검증 경계

- 확장 세트는 지원하지 않는다.
- UI 이벤트 → 엔진 호출 경로에 자동 테스트가 없다. 현재는 수동 플레이로 검증한다.

## 다음 작업

### 입문편(First Steps) 지원

입문편(`firstSteps`) 7편은 `data/first-steps-scripts.json`에 기본편과 분리해
보존한다. 카탈로그 ID·출처 오버레이·참극 세트별 검증 경계를 분명하게 유지하기
위해서다. 2026-08-10에 런타임 지원까지 완료했다.

원본 각본에는 최상위 `loops`가 없지만 모든 항목에 `difficultySets`가 있고 각
난이도 항목이 `numberOfLoops`를 가진다. 따라서 3루프를 세트 전체 기본값으로
추정하지 않고 선택한 난이도에서 루프 수를 읽는다. 첫 항목을 기본 선택으로 쓰되
나머지 변형도 삭제하지 않는다. 공식 `Prevailing Secrecy`부터 기본 항목이 4루프이고
3루프는 더 높은 난이도 변형이므로 전역 3루프 기본값은 원본과 충돌한다.

- `data/tragedy-sets.json`은 원본 `tragedys.jsonc`의 룰·사건 풀과
  `numberOfMainPlots`/`numberOfSubPlots`를 보존한다. 검증기와 UI의 역할 풀은 이
  정의를 참조하며 입문편 룰 X 1개, 기본편 룰 X 2개를 따로 하드코딩하지 않는다.
- 입문편은 최후의 싸움이 없다. 최종 루프 패배 시 즉시 각본가 승리로 끝나지만,
  기존 `추가 루프 (하우스 룰)` 경로는 최종 판정 전에 그대로 제공한다.
- 원본 기본편 22편도 모두 `difficultySets`가 있으며 16편은 복수 변형이다. 입문편과
  기본편 모두 선택 화면에서 난이도별 `numberOfLoops`를 고르고 그 값을
  `Scenario.loops`에 보존한다.
- 입문편의 룰 4종과 골칫거리 역할을 원문 `source` 그대로 등록했다. 복수자의
  등불의 장소 X는 흑막 시작 장소이며, 지켜야 할 장소는 학교 음모 2개를 판정한다.
- 입문편 출처는 설명서 수록 2편만 `official`이다. 나머지 5편은 원본의
  New Tragedies·작성자·외부 출처 메타데이터에 따라 `community`로 분류했고
  `unknown`은 없다.

### 각본 정오표 오버레이와 역할 최대 인원 검증

원본 인쇄물과 upstream 생성 데이터는 보존한다. 게임사가 뒤에 정정한 각본은
`data/errata.json`의 목록형 오버레이로 관리하고, 각본 파싱 후·시나리오 검증 전에
적용한다. 저장 데이터가 정오표 반영 전 역할을 보존한 경우에도 로드하면서 같은
오버레이를 적용한다. 화면에는 `정오표 적용됨`과 역할 변경·출처·확인 근거를 표시해
실물 카드와 화면이 다른 이유를 각본가에게 알린다.

- 첫 보정은 공식 `basicTragedy:12 Those with Antibodies`다. 원본 인쇄의
  `informer = conspiracyTheorist`를 게임사 정오표에 따라 `person`으로 정정한다.
  사용자가 실물로 확인했다.
- upstream 데이터는 잘못 전사한 것이 아니므로 오류 신고 대상이 아니다. upstream에
  전달할 때는 생성 데이터를 바꾸라는 보고가 아니라, 원본을 보존하면서 별도 정오표
  오버레이를 적용하자는 제안으로 남긴다.
- 역할 최대 인원은 활성 룰의 `addsRoles`를 합산한 뒤 역할 `max`로 절삭한 수를
  배정 상한으로 쓴다. 모방자·아웃사이더처럼 `plotLessRole`인 캐릭터는 이 정원에
  포함하지 않는다.
- 정오표 적용 뒤 입문편 7편과 기본편 22편의 모든 난이도 변형을 검사했으며 역할
  최대 인원 위반은 0건이다. 원본 인쇄 상태의 12번만 선동가 2명/상한 1명으로
  실패하는 회귀 테스트를 유지한다.
- 기본편 18번 팬 제작 `Trouble in Paradise`의 아웃사이더 엑스트라 배정은 별도의
  기존 규칙 위반이며 역할 최대 인원 위반이나 정오표 자동 보정 대상으로 취급하지
  않는다.

### 가설 공간 1·2단계 — 관측 모델과 룰 조합 필터

`src/engine/hypothesis.ts`가 참극 세트 정의에서 룰 조합을 완전 열거한다. 입문편은
9개, 기본편은 105개이며 증분 캐시 없이 호출할 때마다 다시 계산한다.

- 역할 공개, 우호 능력 거부, 사건 발생·미발생, 공개된 범인, 정보원이 공개한
  룰 X, 패배 시점, 원인을 숨긴 각본가 능력 결과를 기존 루프 스냅샷·공개 정보·
  단계 기록에서 하나의 `ProtagonistObservation[]`로 파생한다.
- 역할 공개·우호 거부·룰 X 공개와 공개 결과만으로 능력 종류가 확정되는 경우에
  룰 조합을 배제한다. 사건과 패배는 판정 시점 공개 보드 스냅샷이 없으므로 현재는
  관측만 보존하고 필터에는 쓰지 않는다. 확실하지 않은 관측으로 후보를 줄이지
  않는 것이 우선이다.
- 아웃사이더처럼 `plotLessRole`인 캐릭터의 역할 공개와 거부 가능성은 반대로
  판정한다. 활성 룰에 연관된 역할이면 모순이고, 세트 안의 비연관 역할만 가능하다.
- 입문편의 세 룰 Y는 모두 우호 능력을 거부할 수 있는 역할을 추가한다. 따라서
  입문편에서는 거부 관측 하나만으로 9개 후보가 줄지 않는다. 기본편에는 거부를
  설명하지 못하는 조합이 있어 같은 일반 로직으로 배제된다.
- 각 배제 조합은 구조화된 `contradictions`와 한국어 사유를 함께 보존한다.
- P5 각본가 능력 관측은 발동 직전의 공개 장소 음모 스냅샷을 함께 보존한다.
  변수는 학교 음모 2개 이상일 때만 선동가 능력, 도심 음모 2개 이상일 때만
  핵심 인물 능력을 설명할 수 있다. 구 저장처럼 스냅샷이 없으면 근거 없이
  배제하지 않고 보수적으로 후보를 유지한다.
- P5 장소 음모·캐릭터 불안·캐릭터 음모와 P9 사망은 관측 시점의 공개 위치 및
  거물 세력권에서 역할별 원인 캐릭터 집합을 만든다. 여러 루프의 관측은 역할별로
  동시에 만족해야 한다. 역할이 1명이면 후보 집합 교집합, 여러 명이면 모든 후보
  집합을 덮는 최소 인원 수를 선택 룰 조합의 역할 수 상한과 비교한다. 이 판정은
  관측 원인 역할만 다루며 전체 역할 배정 열거나 일반 CSP는 아직 하지 않는다.
- 역할 수는 선택된 룰의 `addsRoles`를 합산한 뒤 역할 `max`로 제한한다. 따라서
  `circleFriends`와 다른 룰이 선동가 또는 친구를 중복 추가해도 각각 최대 1명,
  2명까지만 원인 후보로 쓸 수 있다.
- 장소 음모는 흑막 또는 `unsettlingRumor`, 캐릭터 불안은 선동가 또는 학교 음모
  2개 이상일 때의 변수, 캐릭터 음모는 흑막으로 판정한다. 기자·교사 같은 주인공
  우호 능력은 `goodwillUsed`로 원인이 알려져 있으므로 숨은 원인 관측에서 제외한다.
- 새 `roleReveal` 공개 정보는 공개 순간 역할을 확정 증거로 사용한다. 구 저장의
  `revealedRoleCharacters`만으로 루프 스냅샷에서 복원한 역할은 변이 전후가 불명확해
  확정 증거로 사용하지 않는다.
- `abilityActivated`는 P5 UI 분기에서 직접 만들지 않는다. `resolveHooks()`의 공통
  효과 적용 경로가 훅 하나의 발동 전후 공개 게임판을 비교해, P1~P9뿐 아니라
  `LOOP_START`와 `ON_DEATH`에서도 변화가 있을 때 기록한다. 선택 P5 능력은 효과가
  없어도 발동 기록을 남기던 기존 동작을 유지한다. 사망 효과와 그 뒤 사망 반응은
  서로 다른 훅 관측으로 남는다.
- 관측에는 역할명이나 룰명을 복사하지 않고 공개 시점과 결과만 남긴다. `ON_DEATH`
  직전에 실제 사망한 캐릭터 목록은 공개 트리거로, 결과는 카운터·상태 변화로
  구조화한다. 따라서 관측 문구는 "캐릭터 사망 직후 불안 +6"이지 "연인이 반응"이
  아니다.
- 기본편에서 사망 직후 다른 캐릭터 불안 +6은 `loveAffair`가 없는 조합을 배제해
  `105 → 30`, 루프 시작에 서로 다른 복수 캐릭터 불안 +2는 `threadsFate`가 없는
  조합을 배제해 `105 → 30`이다. 두 관측이 모두 있으면 두 룰 X가 확정되어
  룰 Y 5종만 남는 `105 → 5`다. 사망 트리거가 없거나 루프 시작 +2 대상이 한 명뿐인
  불완전한 패턴은 배제에 쓰지 않는다.
- P9 사망은 발동 직전의 캐릭터 생사·위치·카운터와 거물 세력권처럼 능력이 닿는
  공개 장소를 함께 보존한다. 단 둘인 장소의 사망은 `hiddenFreak`의 연쇄 살인마,
  조건을 만족한 엑스트라가 있을 때는 `paranoiaVirus` 변이도 설명으로 인정한다.
  사망자가 음모 2개 이상이고 같은 장소나 세력권의 다른 캐릭터가 있으면
  `murderPlan`의 살인 청부업자도 가능한 설명으로 합친다. 공개 시점 상태가 없는
  구 기록이나 어느 패턴도 완성되지 않은 사망은 배제하지 않는다.
- 여러 P9 관측은 연쇄 살인마 원인 캐릭터뿐 아니라 살인 청부업자와 사망한 핵심
  인물도 고정 역할 제약으로 함께 본다. `paranoiaVirus`는 관측 시점 불안 3개 이상의
  엑스트라 후보를 별도 원인으로 인정한다.
- 기본편의 순수 단둘 사망은 `105 → 30`, 변이 가능한 엑스트라가 함께 있으면
  `105 → 55`, 단둘이 아니며 청부업자 조건만 맞으면 `105 → 21`이다. 단둘·변이·
  청부업자 조건이 모두 겹치면 가능한 원인의 합집합을 남겨 `105 → 65`다.
  P6 이세계인 우호 능력과 P7 사건처럼 주인공이 이미 원인을 아는 사망은 P9의
  미상 원인 관측으로 변환하지 않는다.
- 친구의 루프 시작 우호 +1은 동일 캐릭터의 친구 역할 공개가 이전 루프에 기록된
  경우만 해석한다. 공개가 없으면 배제하지 않는다. 공개 이력까지 합치면 기본편은
  `circleFriends` 또는 `hiddenFreak`가 있는 `105 → 55`이며, 역할 공개 관측 자체가
  이미 같은 후보를 남기므로 우호 +1의 순차 추가 배제는 0이다.
- P4 해결 기록은 공개 카드 6장, 해결 직전 공개 보드, 실제 공개 변화를 구조화한다.
  주인공의 음모 금지가 정확히 1장이고 같은 대상의 각본가 음모 증가가 실제
  적용됐을 때만 `음모 금지 무시` 관측을 만든다. 주인공 금지가 2장 이상이면
  라운드 집계 규칙으로 모두 무효이므로 관측을 만들지 않는다. 유효한 무시 관측은
  광신도가 나오는 `sealedItem` 또는 `changeOfFuture`만 남겨 `105 → 42`다.
- 실제 번들 각본 2·4·6·7번의 연쇄 살인마·살인 청부업자·친구·광신도 실행 경로를
  회귀 테스트로 통과시키고, 매 경로에서 실제 룰 조합이 남는지 확인한다. 후보를
  과하게 배제하는 변경은 이 테스트가 실패해야 한다.

표시(3단계)는 각본가 정보 패널에 구현했다. 접힌 상태는 전체 룰 조합의 최초 수와
현재 수만 보여주고, 펼치면 룰 Y·룰 X의 고유 후보 수와 이름, 남은 조합 목록,
모든 관측과 관측별 배제 수를 보여준다. 배제 수가 0인 관측도 `배제 없음`으로
표시한다. 관측별 수는 앞선 관측에서 이미 배제된 조합을 다시 세지 않는 순차
기여도이므로 합계가 전체 감소량과 일치한다. 룰 Y·룰 X 후보는 남은 조합에 실제로
등장하는 고유 이름으로 계산한다. 기본편 조합 목록은 한 번 더 접고, 입문편 9조합은
전부 바로 표시한다. 룰 Y가 하나로 확정되면 패널과 룰 Y 행을 위험 상태로 강조한다.

`룰과 역할` 탭도 같은 공개 추론 스냅샷을 공유한다. 룰 Y는 실제 값 옆에 `확정 · 위험`
또는 `후보 N개`를 표시하고, 역할은 캐릭터별 실제 역할 옆에 `확정` 또는 `후보 N개`를
표시한다. 시나리오 데이터의 `subPlots` 배열 순서는 보존하며 비공개 정보표에는 실제
룰을 `룰 X1`, `룰 X2`로 나눠 표시한다. 다만 기본편의 공개 추리에서는 두 자리의
순서가 의미 없으므로 가설 공간은 계속 `C(7, 2)`인 105개다. 남은 조합 전체의
교집합에 든 룰은 특정 자리가 아니라 `포함 확정`으로 표시한다. 예를 들어
`{A,B}`, `{A,C}`, `{A,D}`라면 A가 룰 X에 포함된 사실만 확정이고 X1/X2 위치는
확정하지 않는다. 확장에서 위치를 공개하는 효과를 지원할 때는 별도의 자리 관측을
추가해야 한다.

390×844 실제 렌더에서 기본편 `105 → 105`, 룰 Y 확정 `5 → 1`, 입문편 9개 전체
목록과 룰 X 부분 확정·역할 확정/후보 칩을 확인했다. 패널과 중첩 목록 summary는
모두 44px이고 문서 `scrollWidth`는 375px였으며 브라우저 경고·오류는 없었다.

### 가설 공간 역할 표 1~3단계 — 구조·확정·역전파

`src/engine/hypothesis.ts`의 `RolePossibilityTable`은 전체 역할 배정을 열거하지 않고
캐릭터별 역할 칸을 `possible | impossible | confirmed`로 보존한다.

- 룰 조합이 줄면 더 이상 어느 조합도 공급하지 않는 역할 열을 제거한다. 공개로
  확정된 역할 열은 룰 후보에서 사라져도 공개 사실을 잃지 않도록 남긴다.
- 아웃사이더는 엑스트라와 활성 룰에 연관된 역할이 불가능하다. AI의 엑스트라 금지
  같은 확정 캐릭터 제약도 칸 사유로 남긴다.
- 정확한 역할 공개는 해당 칸을 확정하고 같은 행의 나머지 칸을 모두 불가능으로
  만든다. 구 저장에서 역할을 추정 복원한 `confirmed: false` 공개는 확정 근거로
  쓰지 않는다.
- 살아있는 조합 전체에서 가능한 역할 상한만큼 확정되면 다른 캐릭터의 같은 역할
  칸을 불가능으로 만든다. 조합별 상한보다 확정 인원이 많으면 그 조합을 배제한다.
- 역할별 가능한 캐릭터 수가 조합의 필수 수량보다 적으면 해당 룰 조합을 배제한다.
  `[0, 2]` 골칫거리처럼 최소 0인 역할은 요구하지 않는다.
- `evaluateRoleTableHypotheses()`가 룰 -> 표 -> 룰을 변화가 없을 때까지 반복한다.
  이 과정은 칸 독립 판정이며 완전 역할 배정, 확률, 경우의 수 계산을 하지 않는다.

### 2026-08-14 플레이 피드백 5·2·3 — 역할표 고정점과 공개 부재 관측

- 한 캐릭터 행에 역할 후보가 하나만 남으면 그 칸을 확정한다. 역할 상한 도달로
  다른 행의 같은 역할을 닫고, 새 단일 후보를 다시 확정하는 과정을 고정점까지
  반복한다.
- 모든 생존 룰 조합이 요구하는 역할의 미확정 필요 인원과 남은 후보 수가 같으면
  그 후보들을 확정한다. 친구 2명 중 1명이 공개 확정되고 나머지 후보가 1명인
  경우도 같은 규칙으로 처리한다.
- P5 캐릭터 불안 증가는 대상의 공개 위치에 능력이 닿는 생존 캐릭터를 선동가
  후보로 교집합한다. 발동자 자신도 후보에 포함한다. 학교 음모가 2개 이상이고
  변수가 가능한 룰 조합이 남아 있으면 변수도 같은 결과를 설명할 수 있으므로
  선동가 위치를 단정하지 않는다. 입문편에는 변수가 없어 Prevailing Secrecy에서
  신사에 혼자 있는 무녀의 자기 대상 불안 +1은 무녀를 선동가로 확정한다.
- 완료된 루프의 `history` 최종 스냅샷에서 사망했지만 해당 루프의 역할 공개가
  없었던 캐릭터는 친구 칸을 불가능으로 만든다. `finishLoop()`가 주인공 사망
  종료에도 `LOOP_END`를 먼저 해결하므로 사망한 친구는 공개 기록이 남는다.
  부활해 최종 상태가 생존이면 이 관측을 만들지 않는다.
- 친구 필수 수량도 후보 수와 비교한다. 따라서 친구 불가 캐릭터가 늘어 친목
  동아리의 2명을 채울 수 없으면 그 조합을 배제하지만, 다른 한 명이 친구일 수
  있으면 숨어 있는 살인귀의 친구 1명 조합은 유지한다.
- 플레이 피드백 4(패배 조건 관측)와 1(캐릭터 속성 표시)은 이 커밋 범위에서
  시작하지 않았다.

### 2026-08-14 플레이 피드백 4·1 — 패배 원인 추론과 캐릭터 속성

- 완료된 루프의 `history`에서 생사·카운터·장소 음모·루프 시작 장소·발생 사건과
  종료 단계를 공개 스냅샷으로 복사한다. `LoopOutcome.losses`의 실제 원인 목록은
  각본가만 아는 값이므로 추론에 사용하지 않는다.
- `protagonistDeath`는 병원 사건·살인 청부업자·연인A만 가능한 원인으로 보고,
  일반 루프 종료 패배는 룰의 루프 종료 조건과 핵심 인물·친구·시간 여행자를
  가능한 원인으로 본다. 신사 음모 2개가 있어도 주인공 사망으로 전달된 경우에는
  봉인된 것이 그 사망을 설명하지 못한다.
- 각 룰 조합에서 공개 상태를 설명할 원인이 하나라도 있으면 유지하고, 동시에
  여러 조건이 성립하면 합집합을 남긴다. 병원 사건이 주인공 사망을 공개적으로
  설명하면 룰 조합은 줄이지 않는다.
- 역할 기반 원인이 전체 조합·후보를 통틀어 단일 캐릭터/역할 한 쌍뿐이면 역할표도
  확정한다. `signWithMe`의 핵심 인물 후보는 `girl` 속성만 허용하며, 도심 음모
  2개 이상이면 변수의 핵심 인물 능력도 패배 원인 후보에 포함한다.
- 각본가 패널은 패배 관측 하나가 새 룰이나 역할을 확정한 경우
  `이번 패배로 ... 확정됨` 경고를 상시 표시한다. 실제 원인 자체를 읽지 않고
  관측 전후 후보 차이로 귀속한다.
- 캐릭터 상세 모달의 속성은 `data/characters.json.tags` 순서를 유지하고
  `data/ko-translations.json`의 `Student/Boy/Girl/Adult/Man/Woman` 등 공식 번역을
  사용한다. 신은 `남성 / 여성` 두 속성을 모두 표시한다.
- 불온한 소문이 살아 있는 동안 장소 음모 증가를 단독 캐릭터의 흑막 확정으로
  쓰지 않는다. 모든 조합에서 불온한 소문이 빠진 뒤에만 흑막 위치 교집합을
  적용한다.

관측 기반 불가능 판정, 사건 x 범인 표, 역할 표 UI는 아직 구현하지 않았다. 이 셋은
각각 후속 4~6단계이며, 판정 불가능한 관측은 가능한 상태로 남긴다는 원칙을 유지한다.

P5·P6·P9 발동 전 정보 누출 경고는 구현했다. 복제 상태에서 실제
`applyHookEffect()`·`resolveGoodwillAbility()`·`setOptionalLossActivation()` 경로를
적용하고 공개 관측과 룰·역할 표 추론을 다시 만든 뒤, 발동 전후 룰 조합 수·룰 Y 수·
새 역할 확정을 비교한다. P5는 기본편 전체 추론 15회가 약 61ms였으므로 모든 대상을
미리 계산하지 않고 대상 선택 뒤 해당 1건만 계산한다. 미발동은 항상
`변화 없음 · 안전` 기준선으로 함께 표시한다.

P6은 일반 우호 무시 역할에만 해결/거부 선택이 있고, 절대 우호 무시는
`선택 불가 · 반드시 거부`, 거부 불가 능력은 `선택 불가 · 거부 불가`로 구분한다.
거부 가정은 해당 캐릭터의 우호 무시 계열 후보와 후보 수를 보여준다. P9는 현재
후보 전체에서 공개 보드가 만족하는 패배 조건 종류의 합집합을 `설명 가능한 조건`으로
표시하며, 실제 시나리오의 조건 수로 오해하지 않도록 한다. 일반 루프 종료와
`effect` 즉시 종료를 구분하므로 핵심 인물 사망 직후에는 루프 종료 전용 룰·친구
조건을 미끼로 세지 않는다. P9 선택 훅과 살인 청부업자·연인A 선택 패배에도 같은
예고를 붙였다. 상시 P9 예고는 100회 측정에서 평균 8.73ms, p95 9.71ms였고 한 렌더의
P9 단계 패널과 각본가 상시 패널은 계산 결과를 공유한다. 루프 시작 선택의 하수인
장소·학자 카운터 예고만 후속 범위로 남아 있다. 이후에도 전체 역할 배정은 열거하지
않고 현재 독립 칸 고정점 추론을 그대로 사용한다.

### 2026-08-18 AI·환상 특성 판정과 REI 메이드 조사

- AI가 범인인 사건의 발생 판정값은 캐릭터 위 `goodwill + paranoia + intrigue +
  protection`이다. 원문이 예외 없이 모든 카운터라고 하므로 경찰의 우호 능력으로
  놓는 보호 카운터도 포함한다. 다른 범인은 기존대로 불안만 센다.
- AI [우호3]은 예정 날짜·발생 여부와 관계없이 시나리오의 사건 회차를 고르고,
  AI를 범인으로 효과만 해결한다. 발생 이력과 발생 부수효과는 남기지 않으므로
  나비의 날갯짓 효과를 골라도 미래 변경 계획은 성립하지 않는다. 반대로 사건 효과가
  장소 X의 음모를 실제로 2개 이상 만들면 거대 시한폭탄은 성립한다. 일정 표에는
  예정 사건을 그대로 남기고 실제 선행 해결 날짜를 보조 행으로 표시한다.
- 환상에게 직접 행동 카드를 놓는 것은 금지한다. 살아 있는 환상의 장소에 놓인 카드는
  카드 종류와 진영에 관계없이 환상에게도 적용한다. 기본편 시나리오 설명에도 장소에
  놓인 불안 카드가 환상에게 적용되는 예가 있으므로, 장소에는 본래 효과가 없는 이동·
  우호·불안 카드도 각본가 블러핑으로 놓였다면 환상에게 적용한다. 복사된 음모 금지는
  적용 대상만 늘리고 라운드에 낸 카드 수는 늘리지 않는다.
- 환상 [우호4]는 해결 시 환상을 `absent`로 바꾸고, 기존 `initLoop()` 경로가 다음
  루프 준비에서 시작 장소의 `alive` 상태로 되돌린다. 한국어판 26명·우호 능력
  35건을 실제 엔진 분기와 다시 대조해 32건 지원, 3건 미지원으로 고정했다.
  미지원 3건은 UI에 표시하되 해결·거부 버튼을 모두 비활성화하고 `미구현`을 표시한다.
  엔진 직접 호출도 같은 지원 목록으로 거부하므로 UI와 엔진 판정이 어긋나지 않는다.
- 메이드는 한국 미발매이며 REI 지원 범위다. 데이터 원문에는 재벌가 손녀 또는 흑막이
  같은 장소에서 이동하면 메이드 자신의 이동을 무시하고 강제로 함께 이동하며, 서로 다른
  목적지가 생길 때만 리더가 하나를 고르는 특성이 있다. 확장 지원 시 이동 합성보다 뒤에서
  덧붙이는 것으로 추측하지 말고, 이 동시 이동과 메이드 자신의 이동 무시 타이밍을 별도
  검증해야 한다.
  같은 원문에는 두 인물 중 한 명이 메이드의 장소에서 사망하면 메이드가 대신 사망하는
  효과도 있다.

### 2026-08-24 시체 능력·긴 목록·각본 기억 정보

- 역할·특성 훅의 일반 공급자는 살아 있는 캐릭터로 제한한다. 단, 핵심 인물의
  사망 즉시 종료와 친구의 루프 종료 역할 공개는 시체의 능력 사용이 아니라 사망
  상태에 따른 강제 처리이므로 유지한다. P6 우호 능력 사용자도 시체·부재면 목록에서
  제외하지만, 살아 있는 이세계인·감식관이 시체를 대상으로 삼는 경로는 유지한다.
- 정보 목록은 4건까지 바로 보이고 5번째부터 공통 아코디언에 넣는다. 룰 후보의
  `배제 없음` 관측은 건수만 기본 표시하고 전체를 접는다. 역할 추론, 역할·범인 후보,
  패배 위험 경로가 같은 기준을 사용한다. 진행 기록과 지침 A~E는 기존의 상위·하위
  기본 닫힘 구조를 유지한다.
- `Scenario.specialRules`에는 원본 각본의 비어 있지 않은 특수 규칙만 보존한다.
  룰과 역할 패널은 거물 세력권, 신·전학생 등장 시점, 이번 루프 하수인 시작 장소와
  학자 카운터를 각 캐릭터 행에 표시한다. 하수인·학자 선택 전에는 `미선택`으로
  드러내며 임의 기본값을 만들지 않는다.

### 2026-08-28 추론 결론·근거 묶음

- 역할 추론 과정은 `(결론, 조건)`이 같은 항목을 한 행으로 묶는다. 날짜가 다른 같은
  종류의 근거는 발생 시점만 누적하고, 근거 종류가 다르면 한 결론 안에서 종류별로
  나눈다. 조건부 제약은 조건 문자열까지 같을 때만 합치며 조건이 다르면 별도 결론으로
  남긴다. 기본 화면에는 결론과 근거 건수만 두고 종류별 요약과 개별 원문은 단계적으로
  펼친다.
- 룰 후보 관측 목록도 같은 공개 관측 결과를 묶어 관측 횟수·시점을 접어 둔다. 순차
  필터에서 각 관측이 새로 배제한 조합 수는 합산해 표시한다. `배제 없음 N건`의 N은
  묶인 행 수가 아니라 실제 관측 수를 유지한다.
- 역할별 후보 제약과 사건 범인표는 이미 `(캐릭터, 역할/사건, 판정)`당 한 셀이므로
  행 구조를 더 합치지 않고 동일 사유 라벨만 중복 제거한다. 진행 기록은 실제 상태
  변화의 시간 순서와 루프·날짜 경계가 정보이므로 날짜가 다른 기록을 합치지 않는다.

### 진행 기록 필터

진행 기록을 캐릭터별·장소별로 필터링해 관련 기록만 모아 볼 수 있게 한다.
"무녀에게 무슨 일이 있었나", "신사에서 무슨 일이 있었나"를 한눈에 보기 위한
기능이다.

가설 공간의 역할 배정 층과 연결한다. 특정 캐릭터에 관한 관측을 모으면 해당
캐릭터의 역할 후보를 좁히는 근거가 되므로, 필터 결과에 역할 후보를 함께 표시하는
형태로 확장할 수 있다.

필터 구현 전에 기록 데이터의 캐릭터·장소 참조가 구조화되어 있는지 확인한다.
현재 기록이 문자열 위주라면 구조화가 선행되어야 한다.

### 비활성 사유와 개발용 진단 표기

- 비활성 사유는 시나리오 특수 규칙·캐릭터 특성·역할 능력 원문처럼 각본가가 이미
  아는 고정 사실은 짧은 라벨로 표시한다. 카운터·위치·소진·대상 유무처럼 현재 상태가
  원인인 경우에는 그 상태를 구체적으로 표시한다.
- 우호 능력 카드에 노출되던 `used/limit`, `leader/spent`, 후보 배열 등의 진단 문자열은
  실사용 판단에 필요하지 않고 별도 개발 소비자도 없어 개발 모드 토글을 만들지 않고
  데이터 모델과 화면에서 제거했다.
- 훅 목록은 현재 `when`을 만족한 훅만 표시하므로 별도의 "발동 불가" 사유를 반복하는
  화면이 없다. 훅 원문과 발동 뒤 공개 정보 예고는 비활성 사유가 아니므로 유지한다.
- 390×844 손패에서 짧아진 사유가 가로 넘침 없이 한 줄로 남는 것을 확인했다. 생긴
  여백은 터치 영역과 카드 구분에 유리하므로 다른 정보를 추가해 다시 채우지 않는다.

### 루프 시작 선택 전략 조언

루프 시작 선택(하수인 배치, 학자 카운터)에 전략 조언 UI 필요. 가설 공간 구현
이후. 선택이 패배 조건 진척과 정보 노출에 미치는 영향을 보여주는 형태. 밸런스를
위해 특수 규칙으로 하수인 위치를 고정하는 경우가 있다는 점도 감안할 것.

일반 단계 단위 되돌리기는 아직 미구현이다. 다만 효과 적용 전 P4는 진행 불가 복구를
위해 기존 배치를 보존한 채 P3로 돌아갈 수 있다. 그 밖의 단계는 `LoopState` 스냅샷을
단계 진입 시마다 쌓아 직전 단계 하나만 되돌리는 방식으로 후속 구현한다.

### 미사용 요소 조회

각본가가 레드 헤링을 설계할 때 시나리오에 사용되지 않은 역할·룰도 참조할 수
있어야 한다. 현재 UI는 선택된 시나리오의 요소만 보여주므로, 전체 역할·룰을
조회하는 별도 읽기 전용 화면이 필요하다. 이번 UI 개선 범위에서는 구현하지 않았다.

### 시나리오 정발 제목 대조

기본편 시나리오 22개의 영문 제목은 임의로 번역하지 않는다. 각본가 설명서
16~40페이지에 실린 10편부터 정발 한국어 제목을 대조하고, 출처가 확인된 제목만
UI 번역 데이터에 반영해야 한다.

### 390px 실제 렌더 회귀 검사

- 일반 Vitest DOM 환경은 CSS 레이아웃을 계산하지 않으므로 실제 요소 경계 검사를
  단위 테스트로 가장하지 않는다. `npm test`가 Vitest 뒤에
  `npm run test:mobile-layout`을 실행해 설치된 Chrome/Chromium으로 390×844
  iframe을 실제 렌더한다. 브라우저를 자동 탐지하지 못하면 `CHROME_BIN`에 실행
  파일 경로를 지정한다.
- 실제 렌더 검사는 캐릭터 1~3명 분산, 한 장소 4명, 한 장소 11명 상태에서 칩과
  셀의 `getBoundingClientRect()`를 비교한다. 아울러 문서 가로 스크롤, 하단 조작
  독의 본문 가림, 토스트와 조작 독의 겹침, 카드 선택 패널 위 게임판 120px 이상
  노출을 함께 확인한다.
- `test/mobile-layout.html`은 검사 전 기존 로컬 저장 값을 보관하고 완료 또는 실패
  시 복원한다. 경계 비교 함수 자체는 Vitest 단위 테스트로 별도 검증한다.
